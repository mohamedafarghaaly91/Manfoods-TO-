
<style>
.ntd-select-wrap { position:relative; }
.ntd-select {
    appearance:none; -webkit-appearance:none;
    background:var(--card-bg); border:1px solid var(--card-border); color:var(--text);
    border-radius:var(--radius-sm); font-size:13.5px; font-weight:600;
    height:42px; padding:0 34px 0 14px; min-width:110px;
    box-shadow:var(--shadow-sm); cursor:pointer;
}
.ntd-select:focus { outline:none; border-color:var(--red); box-shadow:0 0 0 3px var(--red-light); }
.ntd-select-wrap i { position:absolute; top:50%; right:12px; transform:translateY(-50%); font-size:11px; color:var(--text-3); pointer-events:none; }
[dir="rtl"] .ntd-select { padding:0 14px 0 34px; }
[dir="rtl"] .ntd-select-wrap i { right:auto; left:12px; }

.ntd-multiselect { position:relative; }
.ntd-multiselect-trigger {
    display:flex; align-items:center; gap:10px; justify-content:space-between;
    background:var(--card-bg); border:1px solid var(--card-border); color:var(--text);
    border-radius:var(--radius-sm); font-size:13.5px; font-weight:600;
    height:42px; padding:0 12px 0 14px; min-width:190px;
    box-shadow:var(--shadow-sm); cursor:pointer;
}
.ntd-multiselect-trigger:hover { border-color:var(--card-border-h); }
.ntd-multiselect-trigger i { font-size:11px; color:var(--text-3); flex-shrink:0; }
.ntd-multiselect-chips { white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
.ntd-multiselect-panel {
    display:none; position:absolute; top:calc(100% + 6px); left:0; z-index:50;
    background:var(--card-bg); border:1px solid var(--card-border); border-radius:var(--radius-sm);
    box-shadow:var(--shadow-lg); min-width:230px; padding:8px; max-height:280px; overflow-y:auto;
}
.ntd-multiselect-panel.open { display:block; }
[dir="rtl"] .ntd-multiselect-panel { left:auto; right:0; }
.ntd-multiselect-actions { display:flex; gap:6px; padding:2px 4px 8px; border-bottom:1px solid var(--card-border); margin-bottom:6px; }
.ntd-ms-action {
    flex:1; background:var(--card-bg-2); border:1px solid var(--card-border); color:var(--red);
    border-radius:6px; font-size:11.5px; font-weight:700; padding:6px 8px; cursor:pointer;
}
.ntd-ms-action:hover { background:var(--red-light); }
.ntd-ms-option { display:flex; align-items:center; gap:9px; padding:7px 8px; border-radius:6px; cursor:pointer; font-size:13px; color:var(--text-2); }
.ntd-ms-option:hover { background:var(--card-bg-2); }
.ntd-ms-option input { accent-color:var(--red); width:15px; height:15px; cursor:pointer; }

.ntd-combobox { position:relative; }
.ntd-combobox-input {
    background:var(--card-bg); border:1px solid var(--card-border); color:var(--text);
    border-radius:var(--radius-sm); font-size:13.5px; font-weight:500;
    height:42px; padding:0 14px; min-width:190px; width:100%;
    box-shadow:var(--shadow-sm);
}
.ntd-combobox-input:focus { outline:none; border-color:var(--red); box-shadow:0 0 0 3px var(--red-light); }
.ntd-combobox-panel {
    display:none; position:absolute; top:calc(100% + 6px); left:0; right:0; z-index:50;
    background:var(--card-bg); border:1px solid var(--card-border); border-radius:var(--radius-sm);
    box-shadow:var(--shadow-lg); max-height:260px; overflow-y:auto; padding:6px;
}
.ntd-combobox-panel.open { display:block; }
.ntd-combo-option { padding:8px 10px; border-radius:6px; font-size:13px; color:var(--text-2); cursor:pointer; }
.ntd-combo-option:hover { background:var(--card-bg-2); }
.ntd-combo-option.active { color:var(--red); font-weight:700; background:var(--red-light); }

/* KPI card text is always English by design (unlocalized metric names) —
   isolate it from RTL bidi reordering when the page is in Arabic. */
#ntdKpiCards .kpi-label, #ntdKpiCards .kpi-value { direction:ltr; unicode-bidi:isolate; text-align:left; }

/* Chart.js draws its tick/legend text on <canvas>, which otherwise inherits
   the page's dir="rtl" and reorders numeric-leading labels. */
.chart-body canvas { direction:ltr; }

.tv-gender-body { display:flex; align-items:center; gap:18px; height:220px; }
.tv-gender-canvas-wrap { flex:0 0 44%; height:100%; position:relative; }
.tv-gender-list { flex:1; display:flex; flex-direction:column; gap:10px; }
.tv-gender-row { display:flex; align-items:center; gap:9px; font-size:13px; color:var(--text-2); }
.tv-gender-dot { width:10px; height:10px; border-radius:50%; flex-shrink:0; }
.tv-gender-label { flex:1; }
.tv-gender-value { font-weight:700; color:var(--text); }
.tv-gender-pct { font-weight:500; color:var(--text-3); }
@@media (max-width: 767px) {
    .tv-gender-body { flex-direction:column; height:auto; }
    .tv-gender-canvas-wrap { flex:0 0 auto; width:100%; height:180px; }
}
.tv-gender-canvas-wrap canvas { direction:ltr; }

/* Compact variant used when this card sits side-by-side with "Reasons for
   Early Leaving" (col-6 at all breakpoints) — stay row-oriented and shrink
   text/spacing instead of falling back to the stacked mobile layout, which
   would otherwise make the narrower half-width card look too tall. */
/* Grows to fill whatever height the flex-column card gives it (the card
   sits h-100 next to the taller "Reasons for Early Leaving" chart card in
   the same row) and vertically centers its content instead of leaving
   empty space below it. */
.tv-gender-body-compact { gap:8px; flex:1; min-height:150px; }
.tv-gender-body-compact .tv-gender-canvas-wrap { flex:0 0 40%; height:130px; width:auto; }
.tv-gender-body-compact .tv-gender-row { font-size:11px; gap:6px; }
.tv-gender-body-compact .tv-gender-dot { width:8px; height:8px; }
@@media (max-width: 767px) {
    .tv-gender-body-compact { flex-direction:row; }
}
</style>

<div class="page-header">
    <div>
        <h1 class="page-title">@L["Page_NinetyDay"]</h1>
        <p class="page-subtitle">@L["Page_NinetyDaySub"]</p>
    </div>
    <div class="d-flex gap-2 flex-wrap align-items-end">
        <div class="ntd-select-wrap"><select id="ntdYearSelect" class="ntd-select"></select><i class="bi bi-chevron-down"></i></div>

        <div class="ntd-multiselect" id="ntdMonthsMultiselect" data-label-select="@L["Common_SelectMonths"]" data-label-all="@L["Common_AllMonths"]">
            <button type="button" class="ntd-multiselect-trigger" id="ntdMonthsTrigger">
                <span class="ntd-multiselect-chips" id="ntdMonthsChips">@L["Common_AllMonths"]</span>
                <i class="bi bi-chevron-down"></i>
            </button>
            <div class="ntd-multiselect-panel" id="ntdMonthsPanel">
                <div class="ntd-multiselect-actions">
                    <button type="button" class="ntd-ms-action" id="ntdMonthsSelectAll">@L["Common_SelectAll"]</button>
                    <button type="button" class="ntd-ms-action" id="ntdMonthsClearAll">@L["Common_ClearAll"]</button>
                </div>
                <div id="ntdMonthsOptions"></div>
            </div>
        </div>

        <div class="ntd-combobox" id="ntdStoreCombo">
            <input type="text" class="ntd-combobox-input" id="ntdStoreInput" placeholder="@L["Common_AllStores"]" autocomplete="off">
            <div class="ntd-combobox-panel" id="ntdStorePanel"></div>
        </div>
        <div class="ntd-combobox" id="ntdOmCombo">
            <input type="text" class="ntd-combobox-input" id="ntdOmInput" placeholder="@L["Common_AllOperationManagers"]" autocomplete="off">
            <div class="ntd-combobox-panel" id="ntdOmPanel"></div>
        </div>
        <div class="ntd-combobox" id="ntdOcCombo">
            <input type="text" class="ntd-combobox-input" id="ntdOcInput" placeholder="@L["Common_AllOperationConsultants"]" autocomplete="off">
            <div class="ntd-combobox-panel" id="ntdOcPanel"></div>
        </div>
        <div class="ntd-combobox" id="ntdSocCombo">
            <input type="text" class="ntd-combobox-input" id="ntdSocInput" placeholder="@L["Common_SeniorOperationConsultants"]" autocomplete="off">
            <div class="ntd-combobox-panel" id="ntdSocPanel"></div>
        </div>
        <div class="ntd-combobox" id="ntdOdCombo">
            <input type="text" class="ntd-combobox-input" id="ntdOdInput" placeholder="@L["Common_OperationDirectors"]" autocomplete="off">
            <div class="ntd-combobox-panel" id="ntdOdPanel"></div>
        </div>
        <div class="job-filter-wrap">
            <button type="button" id="ntdJobSelectButton" class="job-filter-button" aria-haspopup="listbox" aria-expanded="false">
                <span id="ntdJobSelectLabel" data-all="@L["Common_JobTitle"]">@L["Common_JobTitle"]</span><i class="bi bi-chevron-down"></i>
            </button>
            <div id="ntdJobSelectPanel" class="job-filter-panel" hidden></div>
        </div>

        <button type="button" class="reset-filters-btn" onclick="ntdResetFilters()">
            <i class="bi bi-arrow-counterclockwise"></i>@L["Common_ResetFilters"]
        </button>
    </div>
</div>

@{
    var ninetyDayGuide = new PageGuideViewModel
    {
        PageKey = "ninetyday",
        Overview = L["Guide_NinetyDay_Overview"],
        FilterNote = L["Guide_NinetyDay_FilterNote"],
        Sections = new List<PageGuideSection>
        {
            new() { Icon = "bi-calendar3", Title = L["Chart_NinetyDayByCohort"], Body = L["Guide_NinetyDay_Chart_ByCohort"] },
            new() { Icon = "bi-graph-up-arrow", Title = L["Chart_NinetyDayTrend"], Body = L["Guide_NinetyDay_Chart_Trend"] },
            new() { Icon = "bi-shop", Title = L["Chart_ByStore"], Body = L["Guide_NinetyDay_Chart_ByStore"] },
            new() { Icon = "bi-chat-left-text", Title = L["NTD_ReasonsEarlyLeaving"], Body = L["Guide_NinetyDay_Chart_Reasons"] },
            new() { Icon = "bi-gender-ambiguous", Title = L["Chart_GenderBreakdown"], Body = L["Guide_NinetyDay_Chart_Gender"] },
            new() { Icon = "bi-person-lines-fill", Title = L["NTD_ByJobTitle"], Body = L["Guide_NinetyDay_Chart_JobTitle"] },
            new() { Icon = "bi-cash-coin", Title = L["NTD_ByPayroll"], Body = L["Guide_NinetyDay_Chart_Payroll"] },
            new() { Icon = "bi-table", Title = L["Common_StoreComparison"], Body = L["Guide_NinetyDay_Chart_StoreTable"] },
            new() { Icon = "bi-people", Title = L["Common_ByOcOm"], Body = L["Guide_NinetyDay_Chart_OcOm"] },
            new() { Icon = "bi-grid-3x3", Title = L["Common_FullDataTable"], Body = L["Guide_NinetyDay_Chart_TrendMatrix"] },
            new() { Icon = "bi-person-x", Title = L["NTD_EarlyLeavers"], Body = L["Guide_NinetyDay_Chart_EarlyLeavers"] },
        }
    };
}
@await Html.PartialAsync("~/Views/Shared/Dashboard/_PageGuide.cshtml", ninetyDayGuide)

<div id="ntdEmptyState" class="card-dark text-center py-5" style="display:none">
    <i class="bi bi-hourglass-split" style="font-size:3rem;color:oklch(0.5 0.18 25);display:block;margin-bottom:1rem"></i>
    <h5 class="mb-2">@L["NTD_NoCohortData"]</h5>
    <p class="text-muted-custom">@L["NTD_NoCohortDataSub"]</p>
</div>

<div id="ntdContent"
     data-kpi-hired="@L["NTD_KpiHired"]"
     data-kpi-hired-desc="@L["NTD_KpiHiredDesc"]"
     data-kpi-left="@L["NTD_KpiLeft"]"
     data-kpi-left-desc="@L["NTD_KpiLeftDesc"]"
     data-kpi-rate="@L["NTD_KpiRate"]"
     data-kpi-rate-desc="@L["NTD_KpiRateDesc"]"
     data-provisional-badge="@L["NTD_ProvisionalBadge"]"
     data-kpi-range-note="@L["NTD_KpiRangeNote"]"
     data-no-early-leavers="@L["NTD_NoEarlyLeavers"]"
     data-early-leavers-suffix="@L["NTD_EarlyLeaversCountSuffix"]"
     data-stores-suffix="@L["Common_StoresCountSuffix"]"
     data-no-matching-stores="@L["Common_NoMatchingStores"]"
     data-no-data="@L["Common_NoData"]"
     data-no-insights="@L["NTD_NoInsights"]"
     data-loading-short="@L["Common_LoadingShort"]"
     data-no-data-available="@L["Common_NoDataAvailable"]">
    <!-- ── Smart Insights ─────────────────────────────────────── -->
    <div class="d-flex align-items-center gap-2 mb-3">
        <div class="section-accent"></div>
        <h5 class="mb-0 fw-bold">@L["Common_SmartInsights"]</h5>
        <span class="badge ms-1" style="background:oklch(0.6 0.16 25);color:#1C1C27;font-size:10px;padding:4px 8px">@L["Common_AutoGenerated"]</span>
    </div>
    <div id="ntdInsightsContainer" class="row g-3 mb-4">
        <div class="col-12 text-center py-3 text-muted">
            <div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.5 0.18 25)"></div>@L["Common_LoadingInsights"]
        </div>
    </div>

    <div id="ntdProvisionalBanner" class="mb-3" style="display:none;background:oklch(0.75 0.16 75 / .12);border-left:3px solid oklch(0.75 0.16 75);border-radius:8px;padding:10px 14px;font-size:12.5px;color:oklch(0.5 0.14 75)">
        <i class="bi bi-info-circle-fill me-1"></i>@L["NTD_Provisional"]
    </div>

    <div id="ntdKpiCards" class="kpi-grid mb-4">
        <div class="kpi-spinner text-center py-3 w-100">
            <div class="spinner-border spinner-border-sm" style="color:oklch(0.5 0.18 25)"></div>
        </div>
    </div>
    <p id="ntdKpiRangeNote" class="text-muted-custom small mb-3" style="margin-top:-10px"></p>

    <div class="chart-card mb-4">
        <div class="chart-header">
            <h6 id="ntdByCohortTitle">@L["Chart_NinetyDayByCohort"]</h6>
            <p>@L["Chart_NinetyDayByCohortSub"]</p>
        </div>
        <div class="chart-body"><canvas id="ntdByCohortChart" style="max-height:240px"></canvas></div>
    </div>

    <div class="chart-card mb-4">
        <div class="chart-header">
            <h6 id="ntdTrendTitle">@L["Chart_NinetyDayTrend"]</h6>
            <p>@L["Chart_NinetyDayTrendSub"]</p>
        </div>
        <div class="chart-body"><canvas id="ntdTrendChart" style="max-height:280px"></canvas></div>
    </div>

    <div class="chart-card mb-4">
        <div class="chart-header"><h6 id="ntdByStoreTitle">@L["Chart_ByStore"]</h6><p>@L["Chart_ByStoreNinetyDaySub"]</p></div>
        <div class="chart-body"><canvas id="ntdByStoreChart"></canvas></div>
    </div>

    <div class="row g-2 g-md-3 mb-4">
        <div class="col-6">
            <div class="chart-card h-100 p-2 p-md-3">
                <div class="chart-header"><h6 id="ntdReasonsTitle">@L["NTD_ReasonsEarlyLeaving"]</h6><p>@L["NTD_ReasonsEarlyLeavingSub"]</p></div>
                <div class="chart-body"><canvas id="ntdReasonsChart"></canvas></div>
            </div>
        </div>
        <div class="col-6">
            <div class="chart-card h-100 p-2 p-md-3 d-flex flex-column">
                <div class="chart-header"><h6 id="ntdGenderTitle">@L["Chart_GenderBreakdown"]</h6><p>@L["NTD_GenderSub"]</p></div>
                <div class="tv-gender-body tv-gender-body-compact">
                    <div class="tv-gender-canvas-wrap"><canvas id="ntdGenderChart"></canvas></div>
                    <div class="tv-gender-list" id="ntdGenderStatsList"></div>
                </div>
            </div>
        </div>
    </div>

    <div class="charts-grid mb-4">
        <div class="chart-card">
            <div class="chart-header"><h6 id="ntdJobTitleTitle">@L["NTD_ByJobTitle"]</h6><p>@L["NTD_ByJobTitleSub"]</p></div>
            <div class="chart-body"><canvas id="ntdJobTitleChart"></canvas></div>
        </div>
        <div class="chart-card">
            <div class="chart-header"><h6 id="ntdPayrollTitle">@L["NTD_ByPayroll"]</h6><p>@L["NTD_ByPayrollSub"]</p></div>
            <div class="chart-body"><canvas id="ntdPayrollGroupChart"></canvas></div>
        </div>
    </div>

    <!-- ── Store Comparison (90-Day) ──────────────────────────── -->
    <div class="d-flex align-items-center justify-content-between mb-3 flex-wrap gap-2">
        <div class="d-flex align-items-center gap-2">
            <div class="section-accent"></div>
            <h5 class="mb-0 fw-bold" id="ntdStoreComparisonTitle">@L["Common_StoreComparison"]</h5>
            <span id="ntdStoreCountBadge" class="badge" style="background:var(--bg-2);color:var(--text-2);font-size:11px"></span>
        </div>
        <input type="text" id="ntdStoreTableSearch" class="form-control form-control-sm"
               placeholder="&#xF52A; @L["Common_SearchStore"]" style="width:220px;border-radius:20px">
    </div>
    <div class="chart-card mb-4 p-0" style="overflow:hidden">
        <div class="table-responsive" style="max-height:400px;overflow-y:auto">
            <table id="ntdStoreTable" class="table table-hover mb-0" style="font-size:13px">
                <thead style="background:var(--bg-2);border-bottom:2px solid var(--card-border);position:sticky;top:0;z-index:1">
                    <tr>
                        <th class="ntd-sort px-3 py-2" data-col="storeName" style="cursor:pointer;min-width:160px">
                            @L["Common_StoreName"] <i class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>
                        </th>
                        <th class="ntd-sort px-3 py-2" data-col="operationConsultant" style="cursor:pointer">
                            @L["Common_OC"] <i class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>
                        </th>
                        <th class="ntd-sort px-3 py-2" data-col="operationManager" style="cursor:pointer">
                            @L["Common_OM"] <i class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>
                        </th>
                        <th class="ntd-sort px-3 py-2 text-center" data-col="totalHires" style="cursor:pointer">
                            @L["NTD_Hires"] <i class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>
                        </th>
                        <th class="ntd-sort px-3 py-2 text-center" data-col="earlyLeavers" style="cursor:pointer">
                            @L["NTD_EarlyLeavers"] <i class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>
                        </th>
                        <th class="ntd-sort px-3 py-2 text-center" data-col="rate" style="cursor:pointer">
                            @L["NTD_Rate"] <i class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>
                        </th>
                    </tr>
                </thead>
                <tbody id="ntdStoreTableBody">
                    <tr><td colspan="6" class="text-center py-4 text-muted">
                        <div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.6 0.22 22)"></div>@L["Common_Loading"]
                    </td></tr>
                </tbody>
            </table>
        </div>
    </div>

    <!-- ── OC / OM Analysis ───────────────────────────────────── -->
    <div class="d-flex align-items-center gap-2 mb-3">
        <div class="section-accent"></div>
        <h5 class="mb-0 fw-bold">@L["Common_ByOcOm"]</h5>
    </div>
    <div class="row g-3 mb-4">
        <div class="col-lg-6">
            <div class="chart-card oc-analysis-card p-0 h-100 d-flex flex-column" style="overflow:hidden;min-height:0">
                <div class="chart-header stat-card-header">
                    <span class="stat-card-icon" style="--icon-color:oklch(0.6 0.22 22)"><i class="bi bi-person-badge"></i></span>
                    <div>
                        <h6 class="mb-0" id="ntdOcTitle">@L["Common_OperationConsultants"]</h6>
                        <p class="mb-0">@L["NTD_RankedByAvgRate"]</p>
                    </div>
                </div>
                <div class="table-responsive" style="flex:1;overflow-y:auto;min-height:0">
                    <table class="table table-hover table-sm mb-0" style="font-size:13px">
                        <thead style="background:var(--bg-2);position:sticky;top:0;z-index:1">
                            <tr>
                                <th class="px-3 py-2">@L["Common_Name"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Stores"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_Hires"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_EarlyLeavers"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_Rate"]</th>
                            </tr>
                        </thead>
                        <tbody id="ntdOcTableBody">
                            <tr><td colspan="5" class="text-center py-3 text-muted">
                                <div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.6 0.22 22)"></div>@L["Common_Loading"]
                            </td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <div class="col-lg-6 d-flex flex-column gap-3">
            <div class="chart-card p-0" style="overflow:hidden">
                <div class="chart-header stat-card-header">
                    <span class="stat-card-icon" style="--icon-color:oklch(0.5 0.18 250)"><i class="bi bi-person-workspace"></i></span>
                    <div>
                        <h6 class="mb-0" id="ntdOdTitle">@L["Common_OperationDirectors"]</h6>
                        <p class="mb-0">@L["NTD_RankedByAvgRate"]</p>
                    </div>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover table-sm mb-0" style="font-size:13px">
                        <thead style="background:var(--bg-2)">
                            <tr>
                                <th class="px-3 py-2">@L["Common_Name"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Stores"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_Hires"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_EarlyLeavers"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_Rate"]</th>
                            </tr>
                        </thead>
                        <tbody id="ntdOdTableBody">
                            <tr><td colspan="5" class="text-center py-3 text-muted">
                                <div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.5 0.18 250)"></div>@L["Common_Loading"]
                            </td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="chart-card p-0" style="overflow:hidden">
                <div class="chart-header stat-card-header">
                    <span class="stat-card-icon" style="--icon-color:oklch(0.6 0.16 25)"><i class="bi bi-person-gear"></i></span>
                    <div>
                        <h6 class="mb-0" id="ntdOmTitle">@L["Common_OperationManagers"]</h6>
                        <p class="mb-0">@L["NTD_RankedByAvgRate"]</p>
                    </div>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover table-sm mb-0" style="font-size:13px">
                        <thead style="background:var(--bg-2)">
                            <tr>
                                <th class="px-3 py-2">@L["Common_Name"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Stores"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_Hires"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_EarlyLeavers"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_Rate"]</th>
                            </tr>
                        </thead>
                        <tbody id="ntdOmTableBody">
                            <tr><td colspan="5" class="text-center py-3 text-muted">
                                <div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.6 0.16 25)"></div>@L["Common_Loading"]
                            </td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="chart-card p-0" style="overflow:hidden">
                <div class="chart-header stat-card-header">
                    <span class="stat-card-icon" style="--icon-color:oklch(0.55 0.2 300)"><i class="bi bi-person-vcard"></i></span>
                    <div>
                        <h6 class="mb-0" id="ntdSocTitle">@L["Common_SeniorOperationConsultants"]</h6>
                        <p class="mb-0">@L["NTD_RankedByAvgRate"]</p>
                    </div>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover table-sm mb-0" style="font-size:13px">
                        <thead style="background:var(--bg-2)">
                            <tr>
                                <th class="px-3 py-2">@L["Common_Name"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Stores"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_Hires"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_EarlyLeavers"]</th>
                                <th class="px-3 py-2 text-center">@L["NTD_Rate"]</th>
                            </tr>
                        </thead>
                        <tbody id="ntdSocTableBody">
                            <tr><td colspan="5" class="text-center py-3 text-muted">
                                <div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.55 0.2 300)"></div>@L["Common_Loading"]
                            </td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- ── Full Data Table (90-Day Trend Matrix) — one row per store, one column
         per hire-cohort month, showing the 90-day early-leave rate ── -->
    <div class="d-flex align-items-center justify-content-between mb-3 flex-wrap gap-2">
        <div class="d-flex align-items-center gap-2">
            <div class="section-accent"></div>
            <h5 class="mb-0 fw-bold" id="ntdTrendMatrixTitle">@L["Common_FullDataTable"]</h5>
            <span class="badge ms-1" style="background:var(--bg-2);color:var(--text-2);font-size:11px">@L["NTD_AllCohortsBadge"]</span>
        </div>
        <input type="text" id="ntdMatrixSearch" class="form-control form-control-sm"
               placeholder="&#xF52A; @L["Common_SearchStoreOc"]" style="width:220px;border-radius:20px">
    </div>
    <div class="chart-card mb-4 p-0" style="overflow:hidden">
        <div id="ntdMatrixScroll" style="max-height:450px;overflow:auto">
            <table id="ntdMatrixTable" class="table table-hover mb-0" style="font-size:12.5px;min-width:600px">
                <thead id="ntdMatrixHead" style="background:var(--bg-2);position:sticky;top:0;z-index:2">
                    <tr>
                        <th class="px-3 py-2" style="min-width:50px;cursor:pointer" data-ndcol="oc"
                            onclick="ndMxSort('oc')">@L["Common_OC"] <i id="ndMxi-oc" class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i></th>
                        <th class="px-3 py-2" style="min-width:160px;cursor:pointer" data-ndcol="store"
                            onclick="ndMxSort('store')">@L["Common_Store"] <i id="ndMxi-store" class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i></th>
                        <!-- period columns injected by JS -->
                        <th class="px-3 py-2 text-center" style="min-width:70px;cursor:pointer;background:oklch(0.5 0.18 25 / .08)" data-ndcol="total"
                            onclick="ndMxSort('total')">@L["Common_TotalUpper"] <i id="ndMxi-total" class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i></th>
                    </tr>
                </thead>
                <tbody id="ntdMatrixBody">
                    <tr><td colspan="3" class="text-center py-4 text-muted">
                        <div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.6 0.22 22)"></div>@L["Common_LoadingShort"]
                    </td></tr>
                </tbody>
            </table>
        </div>
    </div>

    <div class="d-flex align-items-center gap-2 mb-3">
        <div class="section-accent"></div>
        <h5 class="mb-0 fw-bold">@L["NTD_EarlyLeavers"]</h5>
        <span id="ntdLeaversCountBadge" class="badge ms-1" style="background:var(--bg-2);color:var(--text-2);font-size:11px"></span>
    </div>
    <div class="chart-card p-0" style="overflow:hidden">
        <div class="table-responsive" style="max-height:480px;overflow-y:auto">
            <table id="ntdLeaversTable" class="table table-hover mb-0" style="font-size:13px">
                <thead style="background:var(--bg-2);position:sticky;top:0;z-index:1">
                    <tr>
                        <th class="ntd-sort px-3 py-2" data-col="name" style="cursor:pointer">
                            @L["Common_Name"] <i class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>
                        </th>
                        <th class="ntd-sort px-3 py-2" data-col="store" style="cursor:pointer">
                            @L["Common_Store"] <i class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>
                        </th>
                        <th class="ntd-sort px-3 py-2" data-col="jobTitle" style="cursor:pointer">
                            @L["Common_JobTitle"] <i class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>
                        </th>
                        <th class="ntd-sort px-3 py-2" data-col="hireDate" style="cursor:pointer">
                            @L["Common_HireDate"] <i class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>
                        </th>
                        <th class="ntd-sort px-3 py-2" data-col="resignationDate" style="cursor:pointer">
                            @L["Common_ResignationDate"] <i class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>
                        </th>
                        <th class="ntd-sort px-3 py-2" data-col="tenureDays" style="cursor:pointer">
                            @L["Common_TenureDays"] <i class="bi bi-sort-up text-danger" style="font-size:10px"></i>
                        </th>
                    </tr>
                </thead>
                <tbody id="ntdLeaversBody">
                    <tr><td colspan="6" class="text-center py-4 text-muted">
                        <div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.5 0.18 25)"></div>@L["Common_Loading"]
                    </td></tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/chartjs-plugin-datalabels@2.2.0/dist/chartjs-plugin-datalabels.min.js"></script>
<script>Chart.register(ChartDataLabels);</script>
<script src="~/js/chart-colors.js" asp-append-version="true"></script>
<script src="~/js/dashboard.js" asp-append-version="true"></script>
<!-- dashboard.js is included only for shared helpers (rangeFilterBadge, fetchJson) —
     this page has its own Year+Months filter UI, so dashboard.js's period/store
     selects and loadPeriods() no-op here (no matching element IDs on this page). -->
<script>
/* ── 90-Day Turnover — Year + discrete cohort-Months multi-select, plus
   searchable single-select combos for Store/OM/OC (same components as the
   Turnover and Comparisons pages). Store/OM/OC lists on this page are
   global (not period-scoped) since the underlying API already returns
   them unscoped. */
const ntdL = document.getElementById('ntdContent').dataset;
let ntdYear = null, ntdMonths = [], ntdYearMonthsMap = {};
let ntdStoreFilter = '', ntdOmFilter = '', ntdOcFilter = '', ntdSocFilter = '', ntdOdFilter = '';
let ntdJobFilter = [];

function ntdMonthShortName(m) { return new Date(2000, m - 1, 1).toLocaleString('default', { month: 'short' }); }
function ntdMonthLongName(m, y) { return new Date(y, m - 1).toLocaleString('default', { month: 'long', year: 'numeric' }); }
function ntdAnchor() { return ntdMonths.length ? Math.max(...ntdMonths) : null; }

/* Same title-badge helper as Workforce/Turnover, but "is a range active"
   means more than one cohort month checked in this page's own discrete
   Year+Months picker (it doesn't use the continuous From→To filter those
   pages do, so isPeriodRangeActive() from dashboard.js doesn't apply here). */
const ntdTitleBase = {};
function ntdSetTitleBadge(id, affected) {
    const el = document.getElementById(id);
    if (!el) return;
    if (!(id in ntdTitleBase)) ntdTitleBase[id] = el.textContent;
    el.innerHTML = ntdTitleBase[id] + ' ' + rangeFilterBadge(ntdMonths.length > 1, affected);
}

function ntdBuildQuery(extra) {
    const q = new URLSearchParams(extra || {});
    if (ntdYear) q.set('year', ntdYear);
    if (ntdMonths.length) { q.set('months', ntdMonths.join(',')); q.set('month', ntdAnchor()); }
    if (ntdStoreFilter) q.set('store', ntdStoreFilter);
    if (ntdOmFilter)    q.set('om', ntdOmFilter);
    if (ntdOcFilter)    q.set('oc', ntdOcFilter);
    if (ntdSocFilter)   q.set('soc', ntdSocFilter);
    if (ntdOdFilter)    q.set('od', ntdOdFilter);
    if (ntdJobFilter.length) q.set('jobs', ntdJobFilter.join(','));
    return q;
}

async function ntdFetch(path, extraQuery) {
    const r = await fetch(`/api/ninety-day-turnover/${path}?${ntdBuildQuery(extraQuery).toString()}`);
    return r.ok ? r.json() : null;
}

/* Generic searchable single-select combobox ---------------------------- */
function ntdCreateCombobox({ inputId, panelId, onSelect }) {
    const input = document.getElementById(inputId);
    const panel = document.getElementById(panelId);
    const allLabel = input.placeholder;
    let options = [];
    let value = '';

    function render(filterText) {
        const ft = (filterText || '').toLowerCase();
        const filtered = options.filter(o => o.toLowerCase().includes(ft));
        let html = `<div class="ntd-combo-option${value === '' ? ' active' : ''}" data-value="">${allLabel}</div>`;
        html += filtered.map(o => `<div class="ntd-combo-option${o === value ? ' active' : ''}" data-value="${sapEscape(o)}">${sapEscape(o)}</div>`).join('');
        panel.innerHTML = html;
    }
    function open() { panel.classList.add('open'); render(''); input.select(); }
    function close() { panel.classList.remove('open'); input.value = value; }

    panel.addEventListener('mousedown', (e) => {
        const opt = e.target.closest('.ntd-combo-option');
        if (!opt) return;
        e.preventDefault();
        value = opt.dataset.value;
        input.value = value;
        panel.classList.remove('open');
        onSelect(value);
    });
    input.addEventListener('focus', open);
    input.addEventListener('input', () => render(input.value));
    input.addEventListener('blur', () => setTimeout(close, 150));

    return { setOptions(list) { options = list || []; }, setValue(v) { value = v || ''; input.value = value; } };
}

const ntdStoreCombo = ntdCreateCombobox({ inputId: 'ntdStoreInput', panelId: 'ntdStorePanel', onSelect: v => { ntdStoreFilter = v; ntdLoadAll(); } });
const ntdOmCombo    = ntdCreateCombobox({ inputId: 'ntdOmInput',    panelId: 'ntdOmPanel',    onSelect: v => { ntdOmFilter    = v; ntdLoadAll(); ntdLoadTrendMatrix(); } });
const ntdOcCombo    = ntdCreateCombobox({ inputId: 'ntdOcInput',    panelId: 'ntdOcPanel',    onSelect: v => { ntdOcFilter    = v; ntdLoadAll(); ntdLoadTrendMatrix(); } });
const ntdSocCombo   = ntdCreateCombobox({ inputId: 'ntdSocInput',   panelId: 'ntdSocPanel',   onSelect: v => { ntdSocFilter   = v; ntdLoadAll(); ntdLoadTrendMatrix(); } });
const ntdOdCombo    = ntdCreateCombobox({ inputId: 'ntdOdInput',    panelId: 'ntdOdPanel',    onSelect: v => { ntdOdFilter    = v; ntdLoadAll(); ntdLoadTrendMatrix(); } });
const ntdJobButton = document.getElementById('ntdJobSelectButton');
const ntdJobPanel = document.getElementById('ntdJobSelectPanel');
if (ntdJobButton && ntdJobPanel) {
    ntdJobButton.addEventListener('click', e => {
        e.stopPropagation();
        ntdJobPanel.hidden = !ntdJobPanel.hidden;
        ntdJobButton.setAttribute('aria-expanded', String(!ntdJobPanel.hidden));
    });
    ntdJobPanel.addEventListener('change', () => {
        ntdJobFilter = [...ntdJobPanel.querySelectorAll('input:checked')].map(input => input.value);
        const label = document.getElementById('ntdJobSelectLabel');
        label.textContent = ntdJobFilter.length
            ? `${ntdJobFilter.length} ${document.documentElement.dir === 'rtl' ? 'وظائف مختارة' : 'selected'}`
            : label.dataset.all;
        ntdLoadAll();
        ntdLoadTrendMatrix();
    });
    document.addEventListener('click', e => {
        if (!ntdJobPanel.contains(e.target) && !ntdJobButton.contains(e.target)) ntdJobPanel.hidden = true;
    });
}

/* Months multi-select ---------------------------------------------------- */
function ntdBuildMonthsOptions() {
    const container = document.getElementById('ntdMonthsOptions');
    const available = (ntdYearMonthsMap[ntdYear] || []).slice().sort((a, b) => a - b);
    container.innerHTML = available.map(m => `
        <label class="ntd-ms-option">
            <input type="checkbox" value="${m}" ${ntdMonths.includes(m) ? 'checked' : ''}>
            <span>${ntdMonthLongName(m, ntdYear)}</span>
        </label>`).join('');
    container.querySelectorAll('input[type=checkbox]').forEach(cb => {
        cb.addEventListener('change', () => {
            const m = parseInt(cb.value);
            if (cb.checked) { if (!ntdMonths.includes(m)) ntdMonths.push(m); }
            else { ntdMonths = ntdMonths.filter(x => x !== m); }
            ntdMonths.sort((a, b) => a - b);
            ntdRenderMonthsChips();
            ntdOnMonthsChanged();
        });
    });
}

function ntdRenderMonthsChips() {
    const chipsEl = document.getElementById('ntdMonthsChips');
    const labels = document.getElementById('ntdMonthsMultiselect').dataset;
    const available = ntdYearMonthsMap[ntdYear] || [];
    if (!ntdMonths.length) { chipsEl.textContent = labels.labelSelect; return; }
    if (ntdMonths.length === available.length) { chipsEl.textContent = labels.labelAll; return; }
    const names = ntdMonths.map(ntdMonthShortName);
    chipsEl.textContent = names.length <= 2 ? names.join('، ') : `${names[0]}، ${names[1]} +${names.length - 2}`;
}

let ntdMonthsDebounce;
function ntdOnMonthsChanged() {
    clearTimeout(ntdMonthsDebounce);
    ntdMonthsDebounce = setTimeout(() => { if (ntdMonths.length) ntdLoadAll(); }, 250);
}

document.getElementById('ntdMonthsSelectAll').addEventListener('click', () => {
    ntdMonths = (ntdYearMonthsMap[ntdYear] || []).slice().sort((a, b) => a - b);
    ntdBuildMonthsOptions(); ntdRenderMonthsChips(); ntdOnMonthsChanged();
});
document.getElementById('ntdMonthsClearAll').addEventListener('click', () => {
    ntdMonths = [];
    ntdBuildMonthsOptions(); ntdRenderMonthsChips(); ntdOnMonthsChanged();
});
document.getElementById('ntdMonthsTrigger').addEventListener('click', (e) => {
    e.stopPropagation();
    document.getElementById('ntdMonthsPanel').classList.toggle('open');
});
document.addEventListener('click', (e) => {
    if (!document.getElementById('ntdMonthsMultiselect').contains(e.target))
        document.getElementById('ntdMonthsPanel').classList.remove('open');
});

document.getElementById('ntdYearSelect').addEventListener('change', function () {
    ntdYear = parseInt(this.value);
    ntdMonths = (ntdYearMonthsMap[ntdYear] || []).slice().sort((a, b) => a - b);
    ntdBuildMonthsOptions();
    ntdRenderMonthsChips();
    ntdLoadAll();
});

function ntdResetFilters() {
    ntdStoreFilter = ''; ntdOmFilter = ''; ntdOcFilter = ''; ntdSocFilter = ''; ntdOdFilter = '';
    ntdStoreCombo.setValue(''); ntdOmCombo.setValue(''); ntdOcCombo.setValue(''); ntdSocCombo.setValue(''); ntdOdCombo.setValue('');
    ntdJobFilter = [];
    if (ntdJobPanel) {
        ntdJobPanel.querySelectorAll('input').forEach(input => input.checked = false);
        document.getElementById('ntdJobSelectLabel').textContent = document.getElementById('ntdJobSelectLabel').dataset.all;
    }
    const yearSel = document.getElementById('ntdYearSelect');
    if (yearSel && yearSel.options.length) {
        yearSel.selectedIndex = 0;
        yearSel.dispatchEvent(new Event('change'));
    }
    ntdLoadTrendMatrix();
}

/* Initial filter load ------------------------------------------------------ */
async function ntdLoadFilters() {
    const [periods, stores, managers, consultants, seniorConsultants, directors, jobs] = await Promise.all([
        ntdFetch('cohort-periods'),
        fetch('/api/ninety-day-turnover/stores').then(r => r.ok ? r.json() : []),
        fetch('/api/ninety-day-turnover/operation-managers').then(r => r.ok ? r.json() : []),
        fetch('/api/ninety-day-turnover/operation-consultants').then(r => r.ok ? r.json() : []),
        fetch('/api/ninety-day-turnover/senior-operation-consultants').then(r => r.ok ? r.json() : []),
        fetch('/api/ninety-day-turnover/operation-directors').then(r => r.ok ? r.json() : []),
        fetch('/api/dashboard/job-titles').then(r => r.ok ? r.json() : []),
    ]);
    const cohortPeriods = periods || [];

    const hasData = cohortPeriods.length > 0;
    document.getElementById('ntdEmptyState').style.display = hasData ? 'none' : '';
    document.getElementById('ntdContent').style.display = hasData ? '' : 'none';
    if (!hasData) return false;

    ntdYearMonthsMap = {};
    cohortPeriods.forEach(p => { (ntdYearMonthsMap[p.year] = ntdYearMonthsMap[p.year] || []).push(p.month); });
    const years = Object.keys(ntdYearMonthsMap).map(Number).sort((a, b) => b - a);
    const yearSel = document.getElementById('ntdYearSelect');
    yearSel.innerHTML = years.map(y => `<option value="${y}">${y}</option>`).join('');
    ntdYear = years[0];
    yearSel.value = ntdYear;
    ntdMonths = (ntdYearMonthsMap[ntdYear] || []).slice().sort((a, b) => a - b);
    ntdBuildMonthsOptions();
    ntdRenderMonthsChips();

    ntdStoreCombo.setOptions(stores || []);
    ntdOmCombo.setOptions(managers || []);
    ntdOcCombo.setOptions(consultants || []);
    ntdSocCombo.setOptions(seniorConsultants || []);
    ntdOdCombo.setOptions(directors || []);
    if (ntdJobPanel) {
        ntdJobPanel.innerHTML = (jobs || []).map(job => `
            <label class="job-filter-option">
                <input type="checkbox" value="${sapEscape(job)}" ${ntdJobFilter.includes(job) ? 'checked' : ''}>
                <span>${sapEscape(job)}</span>
            </label>`).join('') || '<div class="job-filter-empty">No jobs available</div>';
    }
    return true;
}

function ntdRenderKpis(kpi) {
    const el = document.getElementById('ntdKpiCards');
    if (!kpi) { el.innerHTML = ''; return; }
    el.innerHTML = `
        <div class="kpi-card"><div class="kpi-icon"><i class="bi bi-people-fill"></i></div><div class="kpi-value">${kpi.totalHires}</div><div class="kpi-label">${ntdL.kpiHired}</div><div class="kpi-desc">${ntdL.kpiHiredDesc}</div></div>
        <div class="kpi-card"><div class="kpi-icon text-danger"><i class="bi bi-person-dash-fill"></i></div><div class="kpi-value text-danger">${kpi.earlyLeavers}</div><div class="kpi-label">${ntdL.kpiLeft}</div><div class="kpi-desc">${ntdL.kpiLeftDesc}</div></div>
        <div class="kpi-card"><div class="kpi-icon"><i class="bi bi-graph-up"></i></div><div class="kpi-value">${kpi.rate.toFixed(1)}%${kpi.isProvisional ? ' <span class=\'badge\' style=\'font-size:10px;background:oklch(0.75 0.16 75 / .18);color:oklch(0.5 0.14 75)\'>${ntdL.provisionalBadge}</span>' : ''}</div><div class="kpi-label">${ntdL.kpiRate}</div><div class="kpi-desc">${ntdL.kpiRateDesc}</div></div>
    `;
    document.getElementById('ntdProvisionalBanner').style.display = kpi.isProvisional ? '' : 'none';
}

async function ntdLoadKpi() {
    const noteEl = document.getElementById('ntdKpiRangeNote');
    if (noteEl) noteEl.innerHTML = ntdMonths.length > 1 ? rangeFilterBadge(true, true) + ' ' + ntdL.kpiRangeNote : '';
    if (!ntdMonths.length) { ntdRenderKpis(null); return; }
    const kpi = await ntdFetch('kpi');
    ntdRenderKpis(kpi);
}

/* All-time trend — unaffected by the Months multi-select, same as the
   Turnover page's Monthly Trend / Full Data Table. */
let ntdTrendInstance;
async function ntdLoadTrend() {
    ntdSetTitleBadge('ntdTrendTitle', false);
    const q = new URLSearchParams();
    if (ntdStoreFilter) q.set('store', ntdStoreFilter);
    if (ntdOmFilter)    q.set('om', ntdOmFilter);
    if (ntdOcFilter)    q.set('oc', ntdOcFilter);
    if (ntdSocFilter)   q.set('soc', ntdSocFilter);
    if (ntdOdFilter)    q.set('od', ntdOdFilter);
    if (ntdJobFilter.length) q.set('jobs', ntdJobFilter.join(','));
    const r = await fetch(`/api/ninety-day-turnover/trend?${q.toString()}`);
    const data = r.ok ? await r.json() : [];
    const ctx = document.getElementById('ntdTrendChart');
    if (!ctx) return;
    if (ntdTrendInstance) ntdTrendInstance.destroy();
    ntdTrendInstance = new Chart(ctx, {
        type: 'line',
        data: {
            labels: data.map(d => d.label),
            datasets: [{
                label: ntdL.kpiRate,
                data: data.map(d => d.rate),
                borderColor: ChartColors.SERIES.PRIMARY,
                backgroundColor: ChartColors.SERIES_FILL.PRIMARY,
                segment: { borderDash: ctx => data[ctx.p1DataIndex]?.isProvisional ? [6, 4] : undefined },
                tension: 0.3, fill: true, pointRadius: 4, spanGaps: true
            }]
        },
        options: {
            responsive: true,
            interaction: { mode: 'index', intersect: false },
            plugins: {
                legend: { display: false },
                tooltip: { callbacks: { label: c => ` ${c.formattedValue}%${data[c.dataIndex]?.isProvisional ? ' (provisional)' : ''}` } },
                datalabels: { display: false }
            },
            scales: {
                x: { ticks: { color: 'oklch(0.5 0.03 258)', font: { size: 11 } }, grid: { display: false } },
                y: { ticks: { color: 'oklch(0.5 0.03 258)', font: { size: 11 }, callback: v => v + '%' }, grid: { color: 'oklch(0.92 0.008 255)' } }
            }
        }
    });
}

/* New: 90-Day Rate by Cohort Month — one bar per selected month, computed
   independently (early leavers ÷ hires for that single cohort month). */
let ntdByCohortInstance;
async function ntdLoadByCohort() {
    ntdSetTitleBadge('ntdByCohortTitle', true);
    const ctx = document.getElementById('ntdByCohortChart');
    if (!ctx) return;
    if (ntdByCohortInstance) ntdByCohortInstance.destroy();
    if (!ntdMonths.length) return;
    const sortedMonths = ntdMonths.slice().sort((a, b) => a - b);
    const results = await Promise.all(sortedMonths.map(m => {
        const q = new URLSearchParams({ year: ntdYear, months: m, month: m });
        if (ntdStoreFilter) q.set('store', ntdStoreFilter);
        if (ntdOmFilter)    q.set('om', ntdOmFilter);
        if (ntdOcFilter)    q.set('oc', ntdOcFilter);
        if (ntdSocFilter)   q.set('soc', ntdSocFilter);
        if (ntdOdFilter)    q.set('od', ntdOdFilter);
        if (ntdJobFilter.length) q.set('jobs', ntdJobFilter.join(','));
        return fetch(`/api/ninety-day-turnover/kpi?${q.toString()}`).then(r => r.ok ? r.json() : null);
    }));
    const cohortLabels = sortedMonths.map(ntdMonthShortName);
    ntdByCohortInstance = new Chart(ctx.getContext('2d'), {
        type: 'bar',
        data: {
            labels: cohortLabels,
            datasets: [{ data: results.map(r => r?.rate || 0), backgroundColor: ChartColors.colorsByLabel(cohortLabels, null), borderRadius: 6, maxBarThickness: 46 }]
        },
        options: {
            maintainAspectRatio: false,
            plugins: { legend: { display: false }, tooltip: { callbacks: { label: c => ` ${c.label}: ${c.parsed.y.toFixed(1)}% of hires left within 90 days` } }, datalabels: { anchor: 'center', align: 'center', color: '#fff', font: { size: 11, weight: 'bold' }, formatter: v => v > 0 ? v.toFixed(1) + '%' : '' } },
            scales: {
                x: { grid: { display: false }, ticks: { color: 'oklch(0.5 0.03 258)' } },
                y: { ticks: { color: 'oklch(0.5 0.03 258)', callback: v => v + '%' }, grid: { color: 'oklch(0.92 0.008 255)' } }
            }
        }
    });
}

/* By Store — top 10 stores by early-leaver rate for the selected cohort
   months, colored by severity (same thresholds as the Store Comparison
   table below) so the chart reads consistently with the rest of the page. */
let ntdByStoreInstance;
async function ntdLoadByStore() {
    ntdSetTitleBadge('ntdByStoreTitle', true);
    const ctx = document.getElementById('ntdByStoreChart');
    if (!ctx) return;
    if (ntdByStoreInstance) ntdByStoreInstance.destroy();
    if (!ntdMonths.length) return;
    const all = await ntdFetch('by-store') || [];
    const data = all.slice(0, 10);
    ntdByStoreInstance = new Chart(ctx.getContext('2d'), {
        type: 'bar',
        data: { labels: data.map(d => d.label), datasets: [{ data: data.map(d => d.value), backgroundColor: data.map(d => ntdRateColor(d.value).bg), borderRadius: 4, maxBarThickness: 26 }] },
        options: {
            indexAxis: 'y',
            maintainAspectRatio: false,
            plugins: {
                legend: { display: false },
                tooltip: { callbacks: { label: c => ` ${c.label}: ${c.formattedValue}% of hires left within 90 days` } },
                datalabels: { anchor: 'center', align: 'center', color: '#fff', font: { size: 11, weight: 'bold' }, formatter: v => v > 0 ? v.toFixed(1) + '%' : '' }
            },
            scales: {
                x: { grid: { color: 'oklch(0.92 0.008 255)' }, ticks: { color: 'oklch(0.5 0.03 258)', callback: v => v + '%' } },
                y: { grid: { display: false }, ticks: { color: 'oklch(0.5 0.03 258)' } }
            }
        }
    });
}

let ntdReasonsInstance;
async function ntdLoadReasons() {
    ntdSetTitleBadge('ntdReasonsTitle', true);
    const ctx = document.getElementById('ntdReasonsChart');
    if (!ctx) return;
    if (ntdReasonsInstance) ntdReasonsInstance.destroy();
    if (!ntdMonths.length) return;
    const data = await ntdFetch('reasons') || [];
    if (!data.length) return;
    const ntdReasonLabels = data.map(d => d.label);
    ntdReasonsInstance = new Chart(ctx.getContext('2d'), {
        type: 'doughnut',
        data: { labels: ntdReasonLabels, datasets: [{ data: data.map(d => d.value), backgroundColor: ChartColors.colorsByLabel(ntdReasonLabels), borderWidth: 2, borderColor: 'oklch(1 0 0)' }] },
        options: { cutout: '60%', maintainAspectRatio: false, plugins: { legend: { position: 'bottom', labels: { color: 'oklch(0.5 0.03 258)', boxWidth: 10, boxHeight: 10, padding: 12, font: { size: 11 } } }, tooltip: { callbacks: { label: c => ` ${c.label}: ${c.formattedValue}` } }, datalabels: { color: '#fff', font: { size: 11, weight: '700' }, textShadowBlur: 4, textShadowColor: 'rgba(0,0,0,0.4)', formatter: (v, ctx) => { const t = ctx.chart.data.datasets[0].data.reduce((a, b) => a + b, 0); return t > 0 && v > 0 ? Math.round(v / t * 100) + '%' : ''; } } } }
    });
}

let ntdLeaversData = [], ntdLeaversSortCol = 'tenureDays', ntdLeaversSortAsc = true;
async function ntdLoadLeavers() {
    const badge = document.getElementById('ntdLeaversCountBadge');
    if (!ntdMonths.length) { ntdLeaversData = []; document.getElementById('ntdLeaversBody').innerHTML = ''; badge.textContent = ''; return; }
    ntdLeaversData = await ntdFetch('early-leavers') || [];
    badge.textContent = ntdLeaversData.length + ' ' + ntdL.earlyLeaversSuffix;
    ntdRenderLeaversTable();
}
function ntdRenderLeaversTable() {
    const tbody = document.getElementById('ntdLeaversBody');
    if (!ntdLeaversData.length) { tbody.innerHTML = '<tr><td colspan="6" class="text-center py-4 text-muted">' + ntdL.noEarlyLeavers + '</td></tr>'; return; }
    const rows = ntdLeaversData.slice().sort((a, b) => {
        const va = a[ntdLeaversSortCol], vb = b[ntdLeaversSortCol];
        if (typeof va === 'string') return ntdLeaversSortAsc ? va.localeCompare(vb) : vb.localeCompare(va);
        return ntdLeaversSortAsc ? va - vb : vb - va;
    });
    tbody.innerHTML = rows.map(r => `
        <tr>
            <td class="px-3 py-2 fw-semibold">${sapEscape(r.name)}</td>
            <td class="px-3 py-2">${sapEscape(r.store)}</td>
            <td class="px-3 py-2" style="color:var(--text-2)">${sapEscape(r.jobTitle)}</td>
            <td class="px-3 py-2" style="color:var(--text-2)">${sapEscape(r.hireDate)}</td>
            <td class="px-3 py-2" style="color:var(--text-2)">${sapEscape(r.resignationDate)}</td>
            <td class="px-3 py-2">${r.tenureDays}</td>
        </tr>`).join('');
}

/* By Job Title (early leavers) --------------------------------------- */
let ntdJobTitleInstance;
async function ntdLoadJobTitleChart() {
    ntdSetTitleBadge('ntdJobTitleTitle', true);
    const ctx = document.getElementById('ntdJobTitleChart');
    if (!ctx) return;
    if (ntdJobTitleInstance) ntdJobTitleInstance.destroy();
    if (!ntdMonths.length) return;
    const data = await ntdFetch('job-titles') || [];
    const jobTitleLabels = data.map(d => d.label);
    ntdJobTitleInstance = new Chart(ctx.getContext('2d'), {
        type: 'bar',
        data: { labels: jobTitleLabels, datasets: [{ data: data.map(d => d.value), backgroundColor: ChartColors.colorsByLabel(jobTitleLabels, null), borderRadius: 4, maxBarThickness: 26 }] },
        options: {
            indexAxis: 'y',
            maintainAspectRatio: false,
            plugins: { legend: { display: false }, tooltip: { callbacks: { label: c => ` ${c.label}: ${c.formattedValue} early leaver(s)` } }, datalabels: { anchor: 'center', align: 'center', color: '#fff', font: { size: 11, weight: 'bold' }, formatter: v => v > 0 ? v : '' } },
            scales: {
                x: { grid: { color: 'oklch(0.92 0.008 255)' }, ticks: { color: 'oklch(0.5 0.03 258)' } },
                y: { grid: { display: false }, ticks: { color: 'oklch(0.5 0.03 258)' } }
            }
        }
    });
}

/* By Payroll Group (early leavers) ------------------------------------ */
let ntdPayrollGroupInstance;
async function ntdLoadPayrollGroupChart() {
    ntdSetTitleBadge('ntdPayrollTitle', true);
    const ctx = document.getElementById('ntdPayrollGroupChart');
    if (!ctx) return;
    if (ntdPayrollGroupInstance) ntdPayrollGroupInstance.destroy();
    if (!ntdMonths.length) return;
    const data = await ntdFetch('payroll-groups') || [];
    const payrollLabels = data.map(d => d.label);
    ntdPayrollGroupInstance = new Chart(ctx.getContext('2d'), {
        type: 'bar',
        data: { labels: payrollLabels, datasets: [{ data: data.map(d => d.value), backgroundColor: ChartColors.colorsByLabel(payrollLabels, ChartColors.PAYROLL_GROUP_COLORS), borderRadius: 4, maxBarThickness: 26 }] },
        options: {
            indexAxis: 'y',
            maintainAspectRatio: false,
            plugins: { legend: { display: false }, tooltip: { callbacks: { label: c => ` ${c.label}: ${c.formattedValue} early leaver(s)` } }, datalabels: { anchor: 'center', align: 'center', color: '#fff', font: { size: 11, weight: 'bold' }, formatter: v => v > 0 ? v : '' } },
            scales: {
                x: { grid: { color: 'oklch(0.92 0.008 255)' }, ticks: { color: 'oklch(0.5 0.03 258)' } },
                y: { grid: { display: false }, ticks: { color: 'oklch(0.5 0.03 258)' } }
            }
        }
    });
}

/* Gender breakdown (early leavers) — donut + side list ------------------ */
let ntdGenderInstance;
async function ntdLoadGenderChart() {
    ntdSetTitleBadge('ntdGenderTitle', true);
    const ctx = document.getElementById('ntdGenderChart');
    const list = document.getElementById('ntdGenderStatsList');
    if (!ctx) return;
    if (ntdGenderInstance) ntdGenderInstance.destroy();
    if (!ntdMonths.length) { if (list) list.innerHTML = ''; return; }
    const data = await ntdFetch('gender-breakdown') || [];
    if (!data.length) { if (list) list.innerHTML = ''; return; }
    const ntdGenderLabels = data.map(d => d.label);
    const ntdGenderColors = ChartColors.colorsByLabel(ntdGenderLabels, ChartColors.GENDER_COLORS);
    ntdGenderInstance = new Chart(ctx.getContext('2d'), {
        type: 'doughnut',
        data: { labels: ntdGenderLabels, datasets: [{ data: data.map(d => d.value), backgroundColor: ntdGenderColors, borderWidth: 2, borderColor: 'oklch(1 0 0)' }] },
        options: { cutout: '60%', maintainAspectRatio: false, plugins: { legend: { display: false }, tooltip: { callbacks: { label: c => ` ${c.label}: ${c.formattedValue}` } }, datalabels: { display: false } } }
    });
    if (list) {
        const total = data.reduce((s, d) => s + d.value, 0);
        list.innerHTML = data.map((d, i) => {
            const pct = total > 0 ? Math.round(d.value / total * 100) : 0;
            return `<div class="tv-gender-row">
                <span class="tv-gender-dot" style="background:${ntdGenderColors[i]}"></span>
                <span class="tv-gender-label">${sapEscape(d.label)}</span>
                <span class="tv-gender-value">${d.value} <span class="tv-gender-pct">(${pct}%)</span></span>
            </div>`;
        }).join('');
    }
}

/* Store Comparison (90-day) -------------------------------------------- */
let ntdStoreData = [], ntdStoreSortCol = 'rate', ntdStoreSortAsc = false;

/* Admin-configurable via the Settings page (loaded once in init below). */
let ntdNinetyDayColorRules = null;
let ntdNinetyDayTotalColorRules = null;
function ntdRateColor(rate) {
    const c = ChartColors.rateColor(ntdNinetyDayColorRules, rate);
    return { bg: c.bg || 'transparent', fg: c.fg || 'var(--text)' };
}

function ntdRenderStoreTable() {
    const search = (document.getElementById('ntdStoreTableSearch').value || '').toLowerCase();
    let rows = ntdStoreData.filter(r =>
        r.storeName.toLowerCase().includes(search) ||
        (r.operationConsultant || '').toLowerCase().includes(search) ||
        (r.operationManager    || '').toLowerCase().includes(search)
    );
    rows.sort((a, b) => {
        const va = a[ntdStoreSortCol], vb = b[ntdStoreSortCol];
        if (typeof va === 'string') return ntdStoreSortAsc ? va.localeCompare(vb) : vb.localeCompare(va);
        return ntdStoreSortAsc ? va - vb : vb - va;
    });

    const badge = document.getElementById('ntdStoreCountBadge');
    if (badge) badge.textContent = rows.length + ' ' + ntdL.storesSuffix;

    const tbody = document.getElementById('ntdStoreTableBody');
    if (!rows.length) {
        tbody.innerHTML = '<tr><td colspan="6" class="text-center py-4 text-muted">' + ntdL.noMatchingStores + '</td></tr>';
        return;
    }
    tbody.innerHTML = rows.map(r => {
        const c = ntdRateColor(r.rate);
        const rateBadge = `<span style="background:${c.bg};color:${c.fg};padding:3px 10px;border-radius:12px;font-size:11.5px;font-weight:700">${r.rate.toFixed(1)}%</span>`;
        return `<tr>
            <td class="px-3 py-2 fw-semibold">${sapEscape(r.storeName)}</td>
            <td class="px-3 py-2" style="color:var(--text-2)">${r.operationConsultant ? sapEscape(r.operationConsultant) : '<span class="text-muted">—</span>'}</td>
            <td class="px-3 py-2" style="color:var(--text-2)">${r.operationManager ? sapEscape(r.operationManager) : '<span class="text-muted">—</span>'}</td>
            <td class="px-3 py-2 text-center">${r.totalHires}</td>
            <td class="px-3 py-2 text-center" style="color:${ChartColors.SEMANTIC.BAD};font-weight:600">${r.earlyLeavers}</td>
            <td class="px-3 py-2 text-center">${rateBadge}</td>
        </tr>`;
    }).join('');
}

async function ntdLoadStoreComparison() {
    ntdSetTitleBadge('ntdStoreComparisonTitle', true);
    if (!ntdMonths.length) { ntdStoreData = []; ntdRenderStoreTable(); return; }
    ntdStoreData = await ntdFetch('store-comparison') || [];
    ntdRenderStoreTable();
}

/* OC / OM Analysis (90-day) --------------------------------------------- */
function ntdRenderOcOmTable(tbodyId, rows) {
    const tbody = document.getElementById(tbodyId);
    if (!rows || !rows.length) {
        tbody.innerHTML = '<tr><td colspan="5" class="text-center py-3 text-muted">' + ntdL.noData + '</td></tr>';
        return;
    }
    tbody.innerHTML = rows.map((r, i) => {
        const c     = ntdRateColor(r.avgRate);
        const badge = `<span style="background:${c.bg};color:${c.fg};padding:3px 10px;border-radius:12px;font-size:11.5px;font-weight:700">${r.avgRate.toFixed(1)}%</span>`;
        const rank  = i === 0 ? `<span style="color:${ChartColors.SEMANTIC.GOOD}">▼</span>` : i === 1 ? `<span style="color:${ChartColors.SEMANTIC.WARNING}">▼</span>` : '<span style="color:#aaa">•</span>';
        return `<tr>
            <td class="px-3 py-2">${rank} <span class="fw-semibold">${sapEscape(r.name)}</span></td>
            <td class="px-3 py-2 text-center">${r.storeCount}</td>
            <td class="px-3 py-2 text-center">${r.totalHires}</td>
            <td class="px-3 py-2 text-center" style="color:${ChartColors.SEMANTIC.BAD};font-weight:600">${r.earlyLeavers}</td>
            <td class="px-3 py-2 text-center">${badge}</td>
        </tr>`;
    }).join('');
}

async function ntdLoadOcOmAnalysis() {
    ntdSetTitleBadge('ntdOcTitle', true);
    ntdSetTitleBadge('ntdOdTitle', true);
    ntdSetTitleBadge('ntdOmTitle', true);
    ntdSetTitleBadge('ntdSocTitle', true);
    if (!ntdMonths.length) {
        ['ntdOcTableBody', 'ntdOmTableBody', 'ntdSocTableBody', 'ntdOdTableBody'].forEach(id => ntdRenderOcOmTable(id, []));
        return;
    }
    const data = await ntdFetch('oc-om-analysis') || { ocRows: [], omRows: [], socRows: [], odRows: [] };
    ntdRenderOcOmTable('ntdOcTableBody', data.ocRows);
    ntdRenderOcOmTable('ntdOmTableBody', data.omRows);
    ntdRenderOcOmTable('ntdSocTableBody', data.socRows);
    ntdRenderOcOmTable('ntdOdTableBody', data.odRows);
}

/* Smart Insights ---------------------------------------------------------- */
async function ntdLoadSmartInsights() {
    const container = document.getElementById('ntdInsightsContainer');
    if (!ntdMonths.length) { container.innerHTML = ''; return; }
    const items = await ntdFetch('smart-insights') || [];

    if (!items.length) {
        container.innerHTML = '<div class="col-12"><div class="alert alert-secondary py-2">' + ntdL.noInsights + '</div></div>';
        return;
    }

    const colorMap = {
        danger:    { bg: 'oklch(0.6 0.22 22 / .08)', border: ChartColors.SEMANTIC.BAD, text: ChartColors.SEMANTIC.BAD },
        success:   { bg: 'oklch(0.62 0.15 155 / .1)', border: 'oklch(0.5 0.15 155)', text: 'oklch(0.5 0.15 155)' },
        warning:   { bg: 'oklch(0.75 0.16 75 / .12)', border: ChartColors.SEMANTIC.WARNING, text: 'oklch(0.6 0.14 75)' },
        secondary: { bg: 'var(--bg-2)', border: 'var(--text-3)', text: 'var(--text-2)' }
    };

    container.innerHTML = items.map(item => {
        const c = colorMap[item.color] || colorMap.secondary;
        return `<div class="col-md-6 col-xl-4">
            <div style="background:${c.bg};border-left:4px solid ${c.border};border-radius:8px;padding:14px 16px;height:100%">
                <div class="d-flex align-items-start gap-2">
                    <i class="bi ${item.icon} mt-1 flex-shrink-0" style="color:${c.text};font-size:17px"></i>
                    <div>
                        <div style="font-size:13px;font-weight:700;color:${c.text};margin-bottom:3px">${sapEscape(item.title)}</div>
                        <div style="font-size:12.5px;color:var(--text-2);line-height:1.55">${sapEscape(item.description)}</div>
                    </div>
                </div>
            </div>
        </div>`;
    }).join('');
}

async function ntdLoadAll() {
    await Promise.all([
        ntdLoadKpi(), ntdLoadByCohort(), ntdLoadByStore(), ntdLoadReasons(), ntdLoadLeavers(),
        ntdLoadJobTitleChart(), ntdLoadPayrollGroupChart(), ntdLoadGenderChart(),
        ntdLoadStoreComparison(), ntdLoadOcOmAnalysis(), ntdLoadSmartInsights(),
    ]);
}

/* ── Full Data Table (90-Day Trend Matrix) — same pattern as the Turnover
   page's Full Data Table, but backed by /api/ninety-day-turnover/trend-matrix.
   Only OM/OC scope the matrix (like the Turnover page); the single Store
   combobox above is ignored here since the matrix already lists every store
   as its own row, and the search box narrows that client-side. */
let ndMxData = null;
let ndMxSortCol = 'total';
let ndMxSortAsc = false;

function ndMxFmtPeriod(pk) {
    const [y, m] = pk.split('-');
    return new Date(+y, +m - 1, 1).toLocaleString('default', { month: 'short', year: '2-digit' });
}

function ndMxRateCell(rate, rules = ntdNinetyDayColorRules) {
    if (rate === null || rate === undefined)
        return `<td class="px-2 py-2 text-center text-muted" style="color:#ccc">—</td>`;
    // Reuses ntdRateColor so this stays consistent with every other 90-Day
    // Rate badge on the page (this cell used to have its own hardcoded
    // Turnover-style thresholds, mismatched with the rest of the page).
    const rateColor = ChartColors.rateColor(rules, rate);
    const c = { bg: rateColor.bg || 'transparent', fg: rateColor.fg || 'var(--text)' };
    const style = c.bg === 'transparent'
        ? 'font-weight:700;white-space:nowrap'
        : `background:${c.bg};color:${c.fg};padding:2px 8px;border-radius:10px;font-size:11px;font-weight:700;white-space:nowrap`;
    return `<td class="px-2 py-2 text-center">
        <span style="${style}">${rate.toFixed(1)}%</span>
    </td>`;
}

// Total = sum of this row's already-displayed monthly rate values (not a
// KPI/pooled rate) — reuses the same periodRates used for the monthly cells,
// treating a missing month (—) as 0 rather than skipping the row.
function ndMxTotalRate(row) {
    return ndMxData.periods.reduce((sum, pk) => sum + (row.periodRates[pk] ?? 0), 0);
}

function ndMxBuildHeader(periods) {
    const tr = document.querySelector('#ntdMatrixHead tr');
    const totalTh = tr.querySelector('[data-ndcol="total"]');
    tr.querySelectorAll('[data-ndcol^="p-"]').forEach(el => el.remove());

    periods.forEach(pk => {
        const th = document.createElement('th');
        th.className = 'px-2 py-2 text-center';
        th.style.cssText = 'min-width:72px;cursor:pointer;white-space:nowrap';
        th.dataset.ndcol = `p-${pk}`;
        th.innerHTML = `${ndMxFmtPeriod(pk)} <i id="ndMxi-${pk}" class="bi bi-arrow-down-up text-muted" style="font-size:10px"></i>`;
        th.onclick = () => ndMxSort(`p-${pk}`);
        tr.insertBefore(th, totalTh);
    });
}

function ndMxRender() {
    if (!ndMxData) return;
    const search = (document.getElementById('ntdMatrixSearch').value || '').toLowerCase();

    let rows = ndMxData.rows.filter(r =>
        r.storeName.toLowerCase().includes(search) ||
        (r.operationConsultant || '').toLowerCase().includes(search)
    );

    rows.sort((a, b) => {
        let va, vb;
        if (ndMxSortCol === 'store') { va = a.storeName; vb = b.storeName; }
        else if (ndMxSortCol === 'oc') { va = a.operationConsultant || ''; vb = b.operationConsultant || ''; }
        else if (ndMxSortCol === 'total') { va = ndMxTotalRate(a); vb = ndMxTotalRate(b); }
        else {
            va = a.periodRates[ndMxSortCol.replace('p-', '')] ?? -1;
            vb = b.periodRates[ndMxSortCol.replace('p-', '')] ?? -1;
        }
        if (typeof va === 'string') return ndMxSortAsc ? va.localeCompare(vb) : vb.localeCompare(va);
        return ndMxSortAsc ? va - vb : vb - va;
    });

    const colSpan = 2 + ndMxData.periods.length + 1;
    if (!rows.length) {
        document.getElementById('ntdMatrixBody').innerHTML =
            `<tr><td colspan="${colSpan}" class="text-center py-4 text-muted">${ntdL.noMatchingStores}</td></tr>`;
        return;
    }

    document.getElementById('ntdMatrixBody').innerHTML = rows.map(r => {
        const periodCells = ndMxData.periods.map(pk => ndMxRateCell(r.periodRates[pk])).join('');
        const totalCell = ndMxRateCell(ndMxTotalRate(r), ntdNinetyDayTotalColorRules)
            .replace('<td', '<td style="background:oklch(0.5 0.18 25 / .06)"');
        return `<tr>
            <td class="px-3 py-2" style="color:var(--text-2);white-space:nowrap">${r.operationConsultant ? sapEscape(r.operationConsultant) : '<span class="text-muted">—</span>'}</td>
            <td class="px-3 py-2 fw-semibold" style="white-space:nowrap">${sapEscape(r.storeName)}</td>
            ${periodCells}
            ${totalCell}
        </tr>`;
    }).join('');
}

function ndMxSort(col) {
    if (ndMxSortCol === col) { ndMxSortAsc = !ndMxSortAsc; }
    else { ndMxSortCol = col; ndMxSortAsc = (col === 'store' || col === 'oc'); }
    document.querySelectorAll('#ntdMatrixHead [data-ndcol] i').forEach(i =>
        i.className = 'bi bi-arrow-down-up text-muted');
    const icon = document.querySelector(`#ntdMatrixHead [data-ndcol="${col}"] i`);
    if (icon) icon.className = ndMxSortAsc ? 'bi bi-sort-up text-danger' : 'bi bi-sort-down text-danger';
    ndMxRender();
}

async function ntdLoadTrendMatrix() {
    ntdSetTitleBadge('ntdTrendMatrixTitle', false);
    const body = document.getElementById('ntdMatrixBody');
    body.innerHTML = '<tr><td colspan="4" class="text-center py-4 text-muted"><div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.6 0.22 22)"></div>' + ntdL.loadingShort + '</td></tr>';
    const q = new URLSearchParams();
    if (ntdStoreFilter) q.set('store', ntdStoreFilter);
    if (ntdOmFilter) q.set('om', ntdOmFilter);
    if (ntdOcFilter) q.set('oc', ntdOcFilter);
    if (ntdSocFilter) q.set('soc', ntdSocFilter);
    if (ntdOdFilter) q.set('od', ntdOdFilter);
    if (ntdJobFilter.length) q.set('jobs', ntdJobFilter.join(','));
    const r = await fetch(`/api/ninety-day-turnover/trend-matrix?${q.toString()}`);
    ndMxData = r.ok ? await r.json() : null;
    if (!ndMxData || !ndMxData.rows || !ndMxData.rows.length) {
        body.innerHTML = '<tr><td colspan="4" class="text-center py-4 text-muted">' + ntdL.noDataAvailable + '</td></tr>';
        return;
    }
    ndMxBuildHeader(ndMxData.periods);
    ndMxRender();
}

document.getElementById('ntdMatrixSearch').addEventListener('input', ndMxRender);

/* Store Comparison table — sort headers + search ------------------------ */
document.querySelectorAll('#ntdStoreTable .ntd-sort').forEach(th => {
    th.addEventListener('click', () => {
        const col = th.dataset.col;
        if (ntdStoreSortCol === col) { ntdStoreSortAsc = !ntdStoreSortAsc; }
        else { ntdStoreSortCol = col; ntdStoreSortAsc = (col === 'storeName' || col === 'operationConsultant' || col === 'operationManager'); }
        document.querySelectorAll('#ntdStoreTable .ntd-sort i').forEach(i => i.className = 'bi bi-arrow-down-up text-muted');
        const icon = th.querySelector('i');
        if (icon) icon.className = ntdStoreSortAsc ? 'bi bi-sort-up text-danger' : 'bi bi-sort-down text-danger';
        ntdRenderStoreTable();
    });
});
document.getElementById('ntdStoreTableSearch').addEventListener('input', ntdRenderStoreTable);

/* Early Leavers table — sort headers ------------------------------------ */
document.querySelectorAll('#ntdLeaversTable .ntd-sort').forEach(th => {
    th.addEventListener('click', () => {
        const col = th.dataset.col;
        if (ntdLeaversSortCol === col) { ntdLeaversSortAsc = !ntdLeaversSortAsc; }
        else { ntdLeaversSortCol = col; ntdLeaversSortAsc = (col !== 'tenureDays'); }
        document.querySelectorAll('#ntdLeaversTable .ntd-sort i').forEach(i => i.className = 'bi bi-arrow-down-up text-muted');
        const icon = th.querySelector('i');
        if (icon) icon.className = ntdLeaversSortAsc ? 'bi bi-sort-up text-danger' : 'bi bi-sort-down text-danger';
        ntdRenderLeaversTable();
    });
});

(async function init() {
    const [colorRules, totalColorRules] = await Promise.all([
        ChartColors.loadRateRules('ninety-day'),
        ChartColors.loadRateRules('ninety-day-total')
    ]);
    ntdNinetyDayColorRules = colorRules;
    ntdNinetyDayTotalColorRules = totalColorRules;
    const hasData = await ntdLoadFilters();
    if (hasData) await Promise.all([ntdLoadTrend(), ntdLoadAll(), ntdLoadTrendMatrix()]);
})();
</script>
