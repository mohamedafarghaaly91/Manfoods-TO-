
<style>
.rt-select-wrap { position:relative; }
.rt-select {
    appearance:none; -webkit-appearance:none;
    background:var(--card-bg); border:1px solid var(--card-border); color:var(--text);
    border-radius:var(--radius-sm); font-size:13.5px; font-weight:600;
    height:42px; padding:0 34px 0 14px; min-width:110px;
    box-shadow:var(--shadow-sm); cursor:pointer;
}
.rt-select:focus { outline:none; border-color:var(--red); box-shadow:0 0 0 3px var(--red-light); }
.rt-select-wrap i { position:absolute; top:50%; right:12px; transform:translateY(-50%); font-size:11px; color:var(--text-3); pointer-events:none; }
[dir="rtl"] .rt-select { padding:0 14px 0 34px; }
[dir="rtl"] .rt-select-wrap i { right:auto; left:12px; }

.rt-multiselect { position:relative; }
.rt-multiselect-trigger {
    display:flex; align-items:center; gap:10px; justify-content:space-between;
    background:var(--card-bg); border:1px solid var(--card-border); color:var(--text);
    border-radius:var(--radius-sm); font-size:13.5px; font-weight:600;
    height:42px; padding:0 12px 0 14px; min-width:190px;
    box-shadow:var(--shadow-sm); cursor:pointer;
}
.rt-multiselect-trigger:hover { border-color:var(--card-border-h); }
.rt-multiselect-trigger i { font-size:11px; color:var(--text-3); flex-shrink:0; }
.rt-multiselect-chips { white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
.rt-multiselect-panel {
    display:none; position:absolute; top:calc(100% + 6px); left:0; z-index:50;
    background:var(--card-bg); border:1px solid var(--card-border); border-radius:var(--radius-sm);
    box-shadow:var(--shadow-lg); min-width:230px; padding:8px; max-height:280px; overflow-y:auto;
}
.rt-multiselect-panel.open { display:block; }
[dir="rtl"] .rt-multiselect-panel { left:auto; right:0; }
.rt-multiselect-actions { display:flex; gap:6px; padding:2px 4px 8px; border-bottom:1px solid var(--card-border); margin-bottom:6px; }
.rt-ms-action {
    flex:1; background:var(--card-bg-2); border:1px solid var(--card-border); color:var(--red);
    border-radius:6px; font-size:11.5px; font-weight:700; padding:6px 8px; cursor:pointer;
}
.rt-ms-action:hover { background:var(--red-light); }
.rt-ms-option { display:flex; align-items:center; gap:9px; padding:7px 8px; border-radius:6px; cursor:pointer; font-size:13px; color:var(--text-2); }
.rt-ms-option:hover { background:var(--card-bg-2); }
.rt-ms-option input { accent-color:var(--red); width:15px; height:15px; cursor:pointer; }

.rt-combobox { position:relative; }
.rt-combobox-input {
    background:var(--card-bg); border:1px solid var(--card-border); color:var(--text);
    border-radius:var(--radius-sm); font-size:13.5px; font-weight:500;
    height:42px; padding:0 14px; min-width:190px; width:100%;
    box-shadow:var(--shadow-sm);
}
.rt-combobox-input:focus { outline:none; border-color:var(--red); box-shadow:0 0 0 3px var(--red-light); }
.rt-combobox-panel {
    display:none; position:absolute; top:calc(100% + 6px); left:0; right:0; z-index:50;
    background:var(--card-bg); border:1px solid var(--card-border); border-radius:var(--radius-sm);
    box-shadow:var(--shadow-lg); max-height:260px; overflow-y:auto; padding:6px;
}
.rt-combobox-panel.open { display:block; }
.rt-combo-option { padding:8px 10px; border-radius:6px; font-size:13px; color:var(--text-2); cursor:pointer; }
.rt-combo-option:hover { background:var(--card-bg-2); }
.rt-combo-option.active { color:var(--red); font-weight:700; background:var(--red-light); }

.rt-tenure-bar-row { display:flex; align-items:center; gap:8px; margin-bottom:6px; }
.rt-tenure-bar-track { flex:1; height:8px; border-radius:20px; background:var(--bg-2); overflow:hidden; display:flex; }
.rt-tenure-bar-seg { height:100%; }

/* Chart.js draws its tick/legend text on <canvas>, which otherwise inherits
   the page's dir="rtl" and reorders numeric-leading labels. */
.chart-body canvas { direction:ltr; }

/* KPI milestone card text is always English by design (unlocalized metric
   names) — isolate it from RTL bidi reordering when the page is in Arabic.
   (The Smart Insights themselves are now localized — see RetentionService.) */
#rtMilestoneCards .kpi-label, #rtMilestoneCards .kpi-value { direction:ltr; unicode-bidi:isolate; text-align:left; }
</style>

<div class="page-header">
    <div>
        <h1 class="page-title">@L["Page_Retention"]</h1>
        <p class="page-subtitle">@L["Page_RetentionSub"]</p>
    </div>
    <div class="d-flex gap-2 flex-wrap align-items-end">
        <div class="rt-select-wrap"><select id="rtYearSelect" class="rt-select"></select><i class="bi bi-chevron-down"></i></div>

        <div class="rt-multiselect" id="rtMonthsMultiselect" data-label-select="@L["Common_SelectMonths"]" data-label-all="@L["Common_AllMonths"]">
            <button type="button" class="rt-multiselect-trigger" id="rtMonthsTrigger">
                <span class="rt-multiselect-chips" id="rtMonthsChips">@L["Common_AllMonths"]</span>
                <i class="bi bi-chevron-down"></i>
            </button>
            <div class="rt-multiselect-panel" id="rtMonthsPanel">
                <div class="rt-multiselect-actions">
                    <button type="button" class="rt-ms-action" id="rtMonthsSelectAll">@L["Common_SelectAll"]</button>
                    <button type="button" class="rt-ms-action" id="rtMonthsClearAll">@L["Common_ClearAll"]</button>
                </div>
                <div id="rtMonthsOptions"></div>
            </div>
        </div>

        <div class="rt-combobox" id="rtStoreCombo">
            <input type="text" class="rt-combobox-input" id="rtStoreInput" placeholder="@L["Common_AllStores"]" autocomplete="off">
            <div class="rt-combobox-panel" id="rtStorePanel"></div>
        </div>
        <div class="rt-combobox" id="rtOmCombo">
            <input type="text" class="rt-combobox-input" id="rtOmInput" placeholder="@L["Common_AllOperationManagers"]" autocomplete="off">
            <div class="rt-combobox-panel" id="rtOmPanel"></div>
        </div>
        <div class="rt-combobox" id="rtOcCombo">
            <input type="text" class="rt-combobox-input" id="rtOcInput" placeholder="@L["Common_AllOperationConsultants"]" autocomplete="off">
            <div class="rt-combobox-panel" id="rtOcPanel"></div>
        </div>
        <div class="rt-combobox" id="rtSocCombo">
            <input type="text" class="rt-combobox-input" id="rtSocInput" placeholder="@L["Common_SeniorOperationConsultants"]" autocomplete="off">
            <div class="rt-combobox-panel" id="rtSocPanel"></div>
        </div>
        <div class="rt-combobox" id="rtOdCombo">
            <input type="text" class="rt-combobox-input" id="rtOdInput" placeholder="@L["Common_OperationDirectors"]" autocomplete="off">
            <div class="rt-combobox-panel" id="rtOdPanel"></div>
        </div>

        <button type="button" class="reset-filters-btn" onclick="rtResetFilters()">
            <i class="bi bi-arrow-counterclockwise"></i>@L["Common_ResetFilters"]
        </button>
    </div>
</div>

@{
    var retentionGuide = new PageGuideViewModel
    {
        PageKey = "retention",
        Overview = L["Guide_Retention_Overview"],
        FilterNote = L["Guide_Retention_FilterNote"],
        Sections = new List<PageGuideSection>
        {
            new() { Icon = "bi-graph-up", Title = L["Chart_SurvivalCurve"], Body = L["Guide_Retention_Chart_Survival"] },
            new() { Icon = "bi-trophy", Title = L["Retention_LeaderboardTop"], Body = L["Guide_Retention_Chart_LeaderboardTop"] },
            new() { Icon = "bi-exclamation-triangle", Title = L["Retention_LeaderboardBottom"], Body = L["Guide_Retention_Chart_LeaderboardBottom"] },
            new() { Icon = "bi-shop", Title = L["Retention_TenureByStore"], Body = L["Guide_Retention_Chart_TenureByStore"] },
            new() { Icon = "bi-person-lines-fill", Title = L["Retention_ByJobTitle"], Body = L["Guide_Retention_Chart_JobTitle"] },
            new() { Icon = "bi-gender-ambiguous", Title = L["Retention_ByGender"], Body = L["Guide_Retention_Chart_Gender"] },
            new() { Icon = "bi-people", Title = L["Retention_ByOcOm"], Body = L["Guide_Retention_Chart_OcOm"] },
        }
    };
}
@await Html.PartialAsync("~/Views/Shared/Dashboard/_PageGuide.cshtml", retentionGuide)

<div id="rtEmptyState" class="card-dark text-center py-5" style="display:none">
    <i class="bi bi-shield-check" style="font-size:3rem;color:oklch(0.5 0.18 25);display:block;margin-bottom:1rem"></i>
    <h5 class="mb-2">@L["RT_NoRetentionData"]</h5>
    <p class="text-muted-custom">@L["RT_NoRetentionDataSub"]</p>
</div>

<div id="rtContent"
     data-no-active-headcount="@L["RT_NoActiveHeadcount"]"
     data-milestone-desc="@L["RT_MilestoneCardDesc"]"
     data-active-employees-suffix="@L["RT_ActiveEmployeesSuffix"]"
     data-still-on-team="@L["RT_StillOnTeam"]"
     data-no-data="@L["Common_NoData"]"
     data-no-data-short="@L["Common_NoDataShort"]"
     data-no-insights="@L["RT_NoInsights"]">
    <div class="d-flex align-items-center gap-2 mb-3">
        <i class="bi bi-lightbulb-fill" style="color:oklch(0.75 0.16 75)"></i>
        <h5 class="mb-0 fw-bold">@L["Common_SmartInsights"]</h5>
        <span class="badge ms-1" style="background:var(--bg-2);color:var(--text-2);font-size:11px">@L["Common_AutoGenerated"]</span>
    </div>
    <div class="chart-card mb-4" id="rtInsightsCard">
        <div id="rtInsights">
            <div class="text-center py-3 text-muted">
                <div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.5 0.18 25)"></div>@L["Common_Loading"]
            </div>
        </div>
    </div>

    <div id="rtMilestoneCards" class="kpi-grid mb-4">
        <div class="kpi-spinner text-center py-3 w-100">
            <div class="spinner-border spinner-border-sm" style="color:oklch(0.5 0.18 25)"></div>
        </div>
    </div>

    <div class="chart-card mb-4">
        <div class="chart-header">
            <h6 id="rtSurvivalTitle">@L["Chart_SurvivalCurve"]</h6>
            <p>@L["Chart_SurvivalCurveSub"]</p>
        </div>
        <div class="chart-body"><canvas id="rtSurvivalChart" style="max-height:280px"></canvas></div>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-lg-6">
            <div class="chart-card h-100">
                <div class="chart-header"><h6 id="rtLeaderboardTopTitle">@L["Retention_LeaderboardTop"]</h6><p>@L["Retention_LeaderboardTopSub"]</p></div>
                <div class="chart-body"><canvas id="rtLeaderboardTopChart"></canvas></div>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="chart-card h-100">
                <div class="chart-header"><h6 id="rtLeaderboardBottomTitle">@L["Retention_LeaderboardBottom"]</h6><p>@L["Retention_LeaderboardBottomSub"]</p></div>
                <div class="chart-body"><canvas id="rtLeaderboardBottomChart"></canvas></div>
            </div>
        </div>
    </div>

    <div class="d-flex align-items-center gap-2 mb-3">
        <div class="section-accent"></div>
        <h5 class="mb-0 fw-bold">@L["Retention_TenureByStore"]</h5>
    </div>
    <div class="chart-card mb-4 p-0" style="overflow:hidden">
        <div class="table-responsive" style="max-height:450px;overflow-y:auto">
            <table class="table table-hover mb-0" style="font-size:12.5px">
                <thead style="background:var(--bg-2);position:sticky;top:0;z-index:1">
                    <tr>
                        <th class="px-3 py-2">@L["Common_StoreName"]</th>
                        <th class="px-3 py-2 text-center">@L["Common_Headcount"]</th>
                        <th class="px-3 py-2" style="min-width:260px">@L["RT_TenureMix"]</th>
                    </tr>
                </thead>
                <tbody id="rtTenureTableBody">
                    <tr><td colspan="3" class="text-center py-4 text-muted">
                        <div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.5 0.18 25)"></div>@L["Common_Loading"]
                    </td></tr>
                </tbody>
            </table>
        </div>
    </div>

    <div class="d-flex align-items-center gap-2 mb-3">
        <div class="section-accent"></div>
        <h5 class="mb-0 fw-bold">@L["Retention_ByJobTitleGender"]</h5>
    </div>
    <div class="row g-3 mb-4">
        <div class="col-lg-6">
            <div class="chart-card h-100">
                <div class="chart-header"><h6 id="rtJobTitleTitle">@L["Retention_ByJobTitle"]</h6><p>@L["Retention_ByJobTitleSub"]</p></div>
                <div class="chart-body"><canvas id="rtJobTitleChart"></canvas></div>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="chart-card h-100">
                <div class="chart-header"><h6 id="rtGenderTitle">@L["Retention_ByGender"]</h6><p>@L["Retention_ByGenderSub"]</p></div>
                <div class="chart-body"><canvas id="rtGenderChart"></canvas></div>
            </div>
        </div>
    </div>

    <div class="d-flex align-items-center gap-2 mb-3">
        <div class="section-accent"></div>
        <h5 class="mb-0 fw-bold">@L["Retention_ByOcOm"]</h5>
    </div>
    <div class="row g-3 mb-4">
        <div class="col-lg-6">
            <div class="chart-card oc-analysis-card p-0 h-100 d-flex flex-column" style="overflow:hidden;min-height:0">
                <div class="chart-header stat-card-header">
                    <span class="stat-card-icon" style="--icon-color:oklch(0.6 0.22 22)"><i class="bi bi-person-badge"></i></span>
                    <div><h6 class="mb-0" id="rtOcTitle">@L["Common_OperationConsultants"]</h6><p class="mb-0">@L["Retention_RankedByTenure"]</p></div>
                </div>
                <div class="table-responsive" style="flex:1;overflow-y:auto;min-height:0">
                    <table class="table table-hover table-sm mb-0" style="font-size:13px">
                        <thead style="background:var(--bg-2);position:sticky;top:0;z-index:1">
                            <tr>
                                <th class="px-3 py-2">@L["Common_Name"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Stores"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Headcount"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_AvgTenure"]</th>
                            </tr>
                        </thead>
                        <tbody id="rtOcTableBody">
                            <tr><td colspan="4" class="text-center py-3 text-muted"><div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.6 0.22 22)"></div>@L["Common_Loading"]</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <div class="col-lg-6 d-flex flex-column gap-3">
            <div class="chart-card p-0" style="overflow:hidden">
                <div class="chart-header stat-card-header">
                    <span class="stat-card-icon" style="--icon-color:oklch(0.5 0.18 250)"><i class="bi bi-person-workspace"></i></span>
                    <div><h6 class="mb-0" id="rtOdTitle">@L["Common_OperationDirectors"]</h6><p class="mb-0">@L["Retention_RankedByTenure"]</p></div>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover table-sm mb-0" style="font-size:13px">
                        <thead style="background:var(--bg-2)">
                            <tr>
                                <th class="px-3 py-2">@L["Common_Name"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Stores"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Headcount"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_AvgTenure"]</th>
                            </tr>
                        </thead>
                        <tbody id="rtOdTableBody">
                            <tr><td colspan="4" class="text-center py-3 text-muted"><div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.5 0.18 250)"></div>@L["Common_Loading"]</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="chart-card p-0" style="overflow:hidden">
                <div class="chart-header stat-card-header">
                    <span class="stat-card-icon" style="--icon-color:oklch(0.6 0.16 25)"><i class="bi bi-person-gear"></i></span>
                    <div><h6 class="mb-0" id="rtOmTitle">@L["Common_OperationManagers"]</h6><p class="mb-0">@L["Retention_RankedByTenure"]</p></div>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover table-sm mb-0" style="font-size:13px">
                        <thead style="background:var(--bg-2)">
                            <tr>
                                <th class="px-3 py-2">@L["Common_Name"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Stores"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Headcount"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_AvgTenure"]</th>
                            </tr>
                        </thead>
                        <tbody id="rtOmTableBody">
                            <tr><td colspan="4" class="text-center py-3 text-muted"><div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.6 0.16 25)"></div>@L["Common_Loading"]</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="chart-card p-0" style="overflow:hidden">
                <div class="chart-header stat-card-header">
                    <span class="stat-card-icon" style="--icon-color:oklch(0.55 0.2 300)"><i class="bi bi-person-vcard"></i></span>
                    <div><h6 class="mb-0" id="rtSocMgrTitle">@L["Common_SeniorOperationConsultants"]</h6><p class="mb-0">@L["Retention_RankedByTenure"]</p></div>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover table-sm mb-0" style="font-size:13px">
                        <thead style="background:var(--bg-2)">
                            <tr>
                                <th class="px-3 py-2">@L["Common_Name"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Stores"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_Headcount"]</th>
                                <th class="px-3 py-2 text-center">@L["Common_AvgTenure"]</th>
                            </tr>
                        </thead>
                        <tbody id="rtSocMgrTableBody">
                            <tr><td colspan="4" class="text-center py-3 text-muted"><div class="spinner-border spinner-border-sm me-2" style="color:oklch(0.55 0.2 300)"></div>@L["Common_Loading"]</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-lg-6">
            <div class="chart-card h-100">
                <div class="chart-header"><h6 id="rtStoreTenureTopTitle">@L["Retention_StoreTenureTop"]</h6><p>@L["Retention_StoreTenureTopSub"]</p></div>
                <div class="chart-body"><canvas id="rtStoreTenureTopChart"></canvas></div>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="chart-card h-100">
                <div class="chart-header"><h6 id="rtStoreTenureBottomTitle">@L["Retention_StoreTenureBottom"]</h6><p>@L["Retention_StoreTenureBottomSub"]</p></div>
                <div class="chart-body"><canvas id="rtStoreTenureBottomChart"></canvas></div>
            </div>
        </div>
    </div>
    <div class="row g-3 mb-4">
        <div class="col-lg-6">
            <div class="chart-card h-100">
                <div class="chart-header"><h6 id="rtOcTenureTopTitle">@L["Retention_OcTenureTop"]</h6><p>@L["Retention_OcTenureTopSub"]</p></div>
                <div class="chart-body"><canvas id="rtOcTenureTopChart"></canvas></div>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="chart-card h-100">
                <div class="chart-header"><h6 id="rtOcTenureBottomTitle">@L["Retention_OcTenureBottom"]</h6><p>@L["Retention_OcTenureBottomSub"]</p></div>
                <div class="chart-body"><canvas id="rtOcTenureBottomChart"></canvas></div>
            </div>
        </div>
    </div>

    <div class="d-flex align-items-center gap-2 mb-3">
        <div class="section-accent"></div>
        <h5 class="mb-0 fw-bold">@L["Retention_MoreContext"]</h5>
    </div>
    <div class="row g-3 mb-4">
        <div class="col-lg-6">
            <div class="chart-card h-100">
                <div class="chart-header"><h6 id="rtFirstResignTitle">@L["Retention_TimeToFirstResign"]</h6><p>@L["Retention_TimeToFirstResignSub"]</p></div>
                <div class="chart-body"><canvas id="rtFirstResignChart"></canvas></div>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="chart-card h-100">
                <div class="chart-header"><h6 id="rtHiringVolumeTitle">@L["Retention_HiringVolume"]</h6><p>@L["Retention_HiringVolumeSub"]</p></div>
                <div class="chart-body"><canvas id="rtHiringVolumeChart"></canvas></div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/chartjs-plugin-datalabels@2.2.0/dist/chartjs-plugin-datalabels.min.js"></script>
<script>Chart.register(ChartDataLabels);</script>
<script src="~/js/chart-colors.js" asp-append-version="true"></script>
<script src="~/js/dashboard.js" asp-append-version="true"></script>
<!-- dashboard.js is included only for shared helpers (rangeFilterBadge, fetchJson) —
     this page has its own Year+Months filter UI, so dashboard.js's period/store
     selects and loadPeriods() no-op here (no matching element IDs on this page). -->
<script>
/* ── Retention page — Year + discrete cohort-Months multi-select, same
   filter components as Turnover/Comparisons/90-Day/Workforce. */
const rtL = document.getElementById('rtContent').dataset;
let rtYear = null, rtMonths = [], rtYearMonthsMap = {};
let rtStoreFilter = '', rtOmFilter = '', rtOcFilter = '', rtSocFilter = '', rtOdFilter = '';

function rtMonthShortName(m) { return new Date(2000, m - 1, 1).toLocaleString('default', { month: 'short' }); }
function rtMonthLongName(m, y) { return new Date(y, m - 1).toLocaleString('default', { month: 'long', year: 'numeric' }); }
function rtAnchor() { return rtMonths.length ? Math.max(...rtMonths) : null; }

/* Range-filter badge (dashboard.js) — captures each title's original text
   once, so repeated filter-change reloads don't stack badges. This page's
   filter is a discrete cohort-Months multi-select (like 90-Day Turnover),
   so "range active" means more than one month is checked. */
const rtTitleBase = {};
function rtSetTitleBadge(id, affected) {
    const el = document.getElementById(id);
    if (!el) return;
    if (!(id in rtTitleBase)) rtTitleBase[id] = el.textContent;
    el.innerHTML = rtTitleBase[id] + ' ' + rangeFilterBadge(rtMonths.length > 1, affected);
}

function rtBuildQuery(extra) {
    const q = new URLSearchParams(extra || {});
    if (rtYear) q.set('toYear', rtYear);
    if (rtMonths.length) {
        q.set('months', rtMonths.join(','));
        q.set('toMonth', rtAnchor());
    }
    if (rtStoreFilter) q.set('store', rtStoreFilter);
    if (rtOmFilter)    q.set('om', rtOmFilter);
    if (rtOcFilter)    q.set('oc', rtOcFilter);
    if (rtSocFilter)   q.set('soc', rtSocFilter);
    if (rtOdFilter)    q.set('od', rtOdFilter);
    return q;
}

async function rtFetch(path, extraQuery) {
    const r = await fetch(`/api/retention/${path}?${rtBuildQuery(extraQuery).toString()}`);
    return r.ok ? r.json() : null;
}

/* Generic searchable single-select combobox ---------------------------- */
function rtCreateCombobox({ inputId, panelId, onSelect }) {
    const input = document.getElementById(inputId);
    const panel = document.getElementById(panelId);
    const allLabel = input.placeholder;
    let options = [];
    let value = '';

    function render(filterText) {
        const ft = (filterText || '').toLowerCase();
        const filtered = options.filter(o => o.toLowerCase().includes(ft));
        let html = `<div class="rt-combo-option${value === '' ? ' active' : ''}" data-value="">${allLabel}</div>`;
        html += filtered.map(o => `<div class="rt-combo-option${o === value ? ' active' : ''}" data-value="${sapEscape(o)}">${sapEscape(o)}</div>`).join('');
        panel.innerHTML = html;
    }
    function open() { panel.classList.add('open'); render(''); input.select(); }
    function close() { panel.classList.remove('open'); input.value = value; }

    panel.addEventListener('mousedown', (e) => {
        const opt = e.target.closest('.rt-combo-option');
        if (!opt) return;
        e.preventDefault();
        value = opt.dataset.value;
        input.value = value;
        panel.classList.remove('open');
        onSelect(value);
    });
    input.addEventListener('focus', open);
    input.addEventListener('input', () => render(input.value));
    input.addEventListener('blur', () => setTimeout(close, 150));

    return { setOptions(list) { options = list || []; }, setValue(v) { value = v || ''; input.value = value; } };
}

const rtStoreCombo = rtCreateCombobox({ inputId: 'rtStoreInput', panelId: 'rtStorePanel', onSelect: v => { rtStoreFilter = v; rtLoadAll(); } });
const rtOmCombo    = rtCreateCombobox({ inputId: 'rtOmInput',    panelId: 'rtOmPanel',    onSelect: v => { rtOmFilter    = v; rtLoadAll(); } });
const rtOcCombo    = rtCreateCombobox({ inputId: 'rtOcInput',    panelId: 'rtOcPanel',    onSelect: v => { rtOcFilter    = v; rtLoadAll(); } });
const rtSocCombo   = rtCreateCombobox({ inputId: 'rtSocInput',   panelId: 'rtSocPanel',   onSelect: v => { rtSocFilter   = v; rtLoadAll(); } });
const rtOdCombo    = rtCreateCombobox({ inputId: 'rtOdInput',    panelId: 'rtOdPanel',    onSelect: v => { rtOdFilter    = v; rtLoadAll(); } });

/* Months multi-select ---------------------------------------------------- */
function rtBuildMonthsOptions() {
    const container = document.getElementById('rtMonthsOptions');
    const available = (rtYearMonthsMap[rtYear] || []).slice().sort((a, b) => a - b);
    container.innerHTML = available.map(m => `
        <label class="rt-ms-option">
            <input type="checkbox" value="${m}" ${rtMonths.includes(m) ? 'checked' : ''}>
            <span>${rtMonthLongName(m, rtYear)}</span>
        </label>`).join('');
    container.querySelectorAll('input[type=checkbox]').forEach(cb => {
        cb.addEventListener('change', () => {
            const m = parseInt(cb.value);
            if (cb.checked) { if (!rtMonths.includes(m)) rtMonths.push(m); }
            else { rtMonths = rtMonths.filter(x => x !== m); }
            rtMonths.sort((a, b) => a - b);
            rtRenderMonthsChips();
            rtOnMonthsChanged();
        });
    });
}

function rtRenderMonthsChips() {
    const chipsEl = document.getElementById('rtMonthsChips');
    const labels = document.getElementById('rtMonthsMultiselect').dataset;
    const available = rtYearMonthsMap[rtYear] || [];
    if (!rtMonths.length) { chipsEl.textContent = labels.labelSelect; return; }
    if (rtMonths.length === available.length) { chipsEl.textContent = labels.labelAll; return; }
    const names = rtMonths.map(rtMonthShortName);
    chipsEl.textContent = names.length <= 2 ? names.join('، ') : `${names[0]}، ${names[1]} +${names.length - 2}`;
}

let rtMonthsDebounce;
function rtOnMonthsChanged() {
    clearTimeout(rtMonthsDebounce);
    rtMonthsDebounce = setTimeout(() => { if (rtMonths.length) rtLoadAll(); }, 250);
}

document.getElementById('rtMonthsSelectAll').addEventListener('click', () => {
    rtMonths = (rtYearMonthsMap[rtYear] || []).slice().sort((a, b) => a - b);
    rtBuildMonthsOptions(); rtRenderMonthsChips(); rtOnMonthsChanged();
});
document.getElementById('rtMonthsClearAll').addEventListener('click', () => {
    rtMonths = [];
    rtBuildMonthsOptions(); rtRenderMonthsChips(); rtOnMonthsChanged();
});
document.getElementById('rtMonthsTrigger').addEventListener('click', (e) => {
    e.stopPropagation();
    document.getElementById('rtMonthsPanel').classList.toggle('open');
});
document.addEventListener('click', (e) => {
    if (!document.getElementById('rtMonthsMultiselect').contains(e.target))
        document.getElementById('rtMonthsPanel').classList.remove('open');
});

document.getElementById('rtYearSelect').addEventListener('change', async function () {
    rtYear = parseInt(this.value);
    rtMonths = (rtYearMonthsMap[rtYear] || []).slice().sort((a, b) => a - b);
    rtStoreFilter = ''; rtOmFilter = ''; rtOcFilter = ''; rtSocFilter = ''; rtOdFilter = '';
    rtStoreCombo.setValue(''); rtOmCombo.setValue(''); rtOcCombo.setValue(''); rtSocCombo.setValue(''); rtOdCombo.setValue('');
    rtBuildMonthsOptions(); rtRenderMonthsChips();
    await rtLoadAll();
});

async function rtResetFilters() {
    const yearSel = document.getElementById('rtYearSelect');
    if (yearSel && yearSel.options.length) {
        yearSel.selectedIndex = 0;
        yearSel.dispatchEvent(new Event('change'));
    }
}

/* Initial filter load ------------------------------------------------------ */
async function rtLoadFilters() {
    const [stores, cohortPeriods, managers, consultants, seniorConsultants, directors] = await Promise.all([
        fetch('/api/retention/stores').then(r => r.ok ? r.json() : []),
        fetch('/api/dashboard/available-periods').then(r => r.ok ? r.json() : []),
        fetch('/api/retention/operation-managers').then(r => r.ok ? r.json() : []),
        fetch('/api/retention/operation-consultants').then(r => r.ok ? r.json() : []),
        fetch('/api/retention/senior-operation-consultants').then(r => r.ok ? r.json() : []),
        fetch('/api/retention/operation-directors').then(r => r.ok ? r.json() : []),
    ]);

    const hasData = (stores || []).length > 0;
    document.getElementById('rtEmptyState').style.display = hasData ? 'none' : '';
    document.getElementById('rtContent').style.display = hasData ? '' : 'none';
    if (!hasData) return false;

    rtYearMonthsMap = {};
    (cohortPeriods || []).forEach(p => { (rtYearMonthsMap[p.year] = rtYearMonthsMap[p.year] || []).push(p.month); });
    const years = Object.keys(rtYearMonthsMap).map(Number).sort((a, b) => b - a);
    const yearSel = document.getElementById('rtYearSelect');
    yearSel.innerHTML = years.map(y => `<option value="${y}">${y}</option>`).join('');
    rtYear = years[0];
    yearSel.value = rtYear;
    rtMonths = (rtYearMonthsMap[rtYear] || []).slice().sort((a, b) => a - b);
    rtBuildMonthsOptions();
    rtRenderMonthsChips();

    rtStoreCombo.setOptions(stores || []);
    rtOmCombo.setOptions(managers || []);
    rtOcCombo.setOptions(consultants || []);
    rtSocCombo.setOptions(seniorConsultants || []);
    rtOdCombo.setOptions(directors || []);
    return true;
}

/* KPI cards mirror the "Team Experience by Store" table below: each card is a
   tenure bucket's share of CURRENT active headcount (a snapshot), not a
   survival/eligibility rate — so a bucket nobody has reached yet correctly
   reads 0%, and short-tenure resignations never distort a longer bucket's card. */
function rtRenderMilestones(items) {
    const el = document.getElementById('rtMilestoneCards');
    if (!items) { el.innerHTML = ''; return; }
    const total = items.reduce((s, b) => s + b.value, 0);
    if (!total) { el.innerHTML = '<div class="text-muted small py-3">' + rtL.noActiveHeadcount + '</div>'; return; }
    const byLabel = {};
    items.forEach(b => { byLabel[b.label] = b.value; });
    el.innerHTML = Object.keys(ChartColors.RETENTION_TENURE_BUCKET_COLORS).map(label => {
        const value = byLabel[label] || 0;
        const pct = value / total * 100;
        const color = ChartColors.RETENTION_TENURE_BUCKET_COLORS[label];
        return `<div class="kpi-card">
            <div class="kpi-icon"><i class="bi bi-people-fill" style="color:${color}"></i></div>
            <div class="kpi-value" style="color:${color}">${pct.toFixed(1)}%</div>
            <div class="kpi-label">${label}</div>
            <div class="kpi-desc">${rtL.milestoneDesc}</div>
            <div style="font-size:11px;color:var(--text-3);margin-top:2px">${value}/${total} ${rtL.activeEmployeesSuffix}</div>
        </div>`;
    }).join('');
}

async function rtLoadMilestones() {
    const q = new URLSearchParams();
    if (rtStoreFilter) q.set('store', rtStoreFilter);
    if (rtOmFilter)    q.set('om', rtOmFilter);
    if (rtOcFilter)    q.set('oc', rtOcFilter);
    if (rtSocFilter)   q.set('soc', rtSocFilter);
    if (rtOdFilter)    q.set('od', rtOdFilter);
    const r = await fetch(`/api/retention/tenure-distribution?${q.toString()}`);
    rtRenderMilestones(r.ok ? await r.json() : []);
}

/* Snapshot of today's active team (not the milestone eligibility model) — same
   philosophy as the KPI cards above, so it stays meaningful for a young company
   and ignores the cohort Months filter (badge shows "Latest period only"). */
let rtSurvivalInstance;
async function rtLoadSurvivalCurve() {
    rtSetTitleBadge('rtSurvivalTitle', false);
    const q = new URLSearchParams();
    if (rtStoreFilter) q.set('store', rtStoreFilter);
    if (rtOmFilter)    q.set('om', rtOmFilter);
    if (rtOcFilter)    q.set('oc', rtOcFilter);
    if (rtSocFilter)   q.set('soc', rtSocFilter);
    if (rtOdFilter)    q.set('od', rtOdFilter);
    const r = await fetch(`/api/retention/active-tenure-curve?${q.toString()}`);
    const data = r.ok ? await r.json() : [];
    const ctx = document.getElementById('rtSurvivalChart');
    if (!ctx) return;
    if (rtSurvivalInstance) rtSurvivalInstance.destroy();
    rtSurvivalInstance = new Chart(ctx, {
        type: 'line',
        data: {
            labels: data.map(d => d.label),
            datasets: [{
                label: rtL.stillOnTeam,
                data: data.map(d => d.retentionRate),
                borderColor: 'oklch(0.55 0.19 25)',
                backgroundColor: 'oklch(0.55 0.19 25 / .15)',
                tension: 0.3, fill: true, pointRadius: 4, spanGaps: true
            }]
        },
        options: {
            responsive: true,
            plugins: {
                legend: { display: false },
                tooltip: { callbacks: { afterLabel: c => `Out of ${data[c.dataIndex]?.sampleSize ?? 0} active employees today` } },
                datalabels: { display: false }
            },
            scales: {
                x: { ticks: { color: 'oklch(0.5 0.03 258)', font: { size: 11 } }, grid: { display: false } },
                y: { min: 0, max: 100, ticks: { color: 'oklch(0.5 0.03 258)', font: { size: 11 }, callback: v => v + '%' }, grid: { color: 'oklch(0.92 0.008 255)' } }
            }
        }
    });
}

/* Store Retention Ranking, split into Top 5 / Bottom 5 — easier to read at a
   glance than one long ranked bar list, and puts the stores that actually
   need attention (the worst 5) front and center instead of buried at the
   bottom of a single chart. */
let rtLeaderboardTopInstance, rtLeaderboardBottomInstance;
function rtRenderLeaderboardChart(canvasId, existing, data, barColor) {
    const ctx = document.getElementById(canvasId);
    if (!ctx) return existing;
    if (existing) existing.destroy();
    return new Chart(ctx.getContext('2d'), {
        type: 'bar',
        data: { labels: data.map(d => d.label), datasets: [{ data: data.map(d => d.value), backgroundColor: barColor, borderRadius: 4, maxBarThickness: 30 }] },
        options: {
            indexAxis: 'y',
            plugins: {
                legend: { display: false },
                tooltip: { callbacks: { label: c => ` ${c.label}: ${c.formattedValue}%` } },
                datalabels: { anchor: 'center', align: 'center', color: '#fff', font: { size: 11, weight: 'bold' }, formatter: v => v > 0 ? v.toFixed(1) + '%' : '' }
            },
            scales: {
                x: { min: 0, max: 100, grid: { color: 'oklch(0.92 0.008 255)' }, ticks: { color: 'oklch(0.5 0.03 258)', callback: v => v + '%' } },
                y: { grid: { display: false }, ticks: { color: 'oklch(0.5 0.03 258)' } }
            }
        }
    });
}

async function rtLoadLeaderboard() {
    rtSetTitleBadge('rtLeaderboardTopTitle', false);
    rtSetTitleBadge('rtLeaderboardBottomTitle', false);
    const q = new URLSearchParams();
    if (rtStoreFilter) q.set('store', rtStoreFilter);
    if (rtOmFilter)    q.set('om', rtOmFilter);
    if (rtOcFilter)    q.set('oc', rtOcFilter);
    if (rtSocFilter)   q.set('soc', rtSocFilter);
    if (rtOdFilter)    q.set('od', rtOdFilter);
    const r = await fetch(`/api/retention/store-retention-ranking?${q.toString()}`);
    const data = r.ok ? await r.json() : [];
    // Already ranked best-first by the API.
    const top5 = data.slice(0, 5);
    const bottom5 = data.slice(-5).reverse();
    rtLeaderboardTopInstance = rtRenderLeaderboardChart('rtLeaderboardTopChart', rtLeaderboardTopInstance, top5, ChartColors.SEMANTIC.GOOD);
    rtLeaderboardBottomInstance = rtRenderLeaderboardChart('rtLeaderboardBottomChart', rtLeaderboardBottomInstance, bottom5, ChartColors.SEMANTIC.BAD);
}

/* Team Experience by Store — replaces the old single company-wide doughnut
   with a per-store breakdown table (or a single row when a store is picked). */
function rtRenderTenureTable(rows) {
    const tbody = document.getElementById('rtTenureTableBody');
    if (!rows || !rows.length) { tbody.innerHTML = '<tr><td colspan="3" class="text-center py-4 text-muted">' + rtL.noData + '</td></tr>'; return; }
    tbody.innerHTML = rows.map(r => {
        const total = r.buckets.reduce((s, b) => s + b.value, 0) || 1;
        const bucketColor = b => ChartColors.RETENTION_TENURE_BUCKET_COLORS[b.label] || ChartColors.hashColor(b.label);
        const segs = r.buckets.filter(b => b.value > 0).map((b) => {
            const pct = Math.round(b.value / total * 100);
            return `<span class="rt-tenure-bar-seg" style="width:${pct}%;background:${bucketColor(b)}" title="${b.label}: ${b.value} (${pct}%)"></span>`;
        }).join('');
        const legend = r.buckets.filter(b => b.value > 0).map((b) =>
            `<span style="color:var(--text-3);font-size:10.5px;margin-inline-end:8px"><span style="display:inline-block;width:7px;height:7px;border-radius:50%;background:${bucketColor(b)};margin-inline-end:3px"></span>${b.label} (${b.value})</span>`
        ).join('');
        return `<tr>
            <td class="px-3 py-2 fw-semibold">${sapEscape(r.storeName)}</td>
            <td class="px-3 py-2 text-center">${r.headcount}</td>
            <td class="px-3 py-2">
                <div class="rt-tenure-bar-row"><div class="rt-tenure-bar-track">${segs}</div></div>
                <div>${legend}</div>
            </td>
        </tr>`;
    }).join('');
}

async function rtLoadTenureByStore() {
    const q = new URLSearchParams();
    if (rtStoreFilter) q.set('store', rtStoreFilter);
    if (rtOmFilter)    q.set('om', rtOmFilter);
    if (rtOcFilter)    q.set('oc', rtOcFilter);
    if (rtSocFilter)   q.set('soc', rtSocFilter);
    if (rtOdFilter)    q.set('od', rtOdFilter);
    const r = await fetch(`/api/retention/tenure-distribution-by-store?${q.toString()}`);
    rtRenderTenureTable(r.ok ? await r.json() : []);
}

/* ── Retention by Job Title / Gender — same plain "% of current team past 6
   months" logic as the KPI cards, just sliced a different way. ─────────── */
function rtFilterQuery() {
    const q = new URLSearchParams();
    if (rtStoreFilter) q.set('store', rtStoreFilter);
    if (rtOmFilter)    q.set('om', rtOmFilter);
    if (rtOcFilter)    q.set('oc', rtOcFilter);
    if (rtSocFilter)   q.set('soc', rtSocFilter);
    if (rtOdFilter)    q.set('od', rtOdFilter);
    return q;
}

function rtRenderPctBarChart(canvasId, existing, data, colors) {
    const ctx = document.getElementById(canvasId);
    if (!ctx) return existing;
    if (existing) existing.destroy();
    return new Chart(ctx.getContext('2d'), {
        type: 'bar',
        data: { labels: data.map(d => d.label), datasets: [{ data: data.map(d => d.value), backgroundColor: colors || ChartColors.colorsByLabel(data.map(d => d.label), null), borderRadius: 4, maxBarThickness: 30 }] },
        options: {
            indexAxis: 'y',
            plugins: {
                legend: { display: false },
                tooltip: { callbacks: { label: c => ` ${c.label}: ${c.formattedValue}% still on the job after 6 months` } },
                datalabels: { anchor: 'center', align: 'center', color: '#fff', font: { size: 11, weight: 'bold' }, formatter: v => v > 0 ? v.toFixed(1) + '%' : '' }
            },
            scales: {
                x: { min: 0, max: 100, grid: { color: 'oklch(0.92 0.008 255)' }, ticks: { color: 'oklch(0.5 0.03 258)', callback: v => v + '%' } },
                y: { grid: { display: false }, ticks: { color: 'oklch(0.5 0.03 258)' } }
            }
        }
    });
}

let rtJobTitleInstance;
async function rtLoadJobTitleChart() {
    const r = await fetch(`/api/retention/by-job-title?${rtFilterQuery().toString()}`);
    const data = r.ok ? await r.json() : [];
    rtJobTitleInstance = rtRenderPctBarChart('rtJobTitleChart', rtJobTitleInstance, data);
}

let rtGenderInstance;
async function rtLoadGenderChart() {
    const r = await fetch(`/api/retention/by-gender?${rtFilterQuery().toString()}`);
    const data = r.ok ? await r.json() : [];
    rtGenderInstance = rtRenderPctBarChart('rtGenderChart', rtGenderInstance, data, ChartColors.colorsByLabel(data.map(d => d.label), ChartColors.GENDER_COLORS));
}

/* ── Average Tenure by Operation Consultant & Manager — same table shape as
   the "By Operation Consultant & Manager" section on other pages, but the
   metric is average tenure of the current team instead of turnover. ────── */
function rtRenderManagerTenureRows(tbodyId, rows) {
    const tbody = document.getElementById(tbodyId);
    if (!rows || !rows.length) { tbody.innerHTML = '<tr><td colspan="4" class="text-center py-3 text-muted">' + rtL.noDataShort + '</td></tr>'; return; }
    tbody.innerHTML = rows.map(r => `<tr>
        <td class="px-3 py-2 fw-semibold">${sapEscape(r.name)}</td>
        <td class="px-3 py-2 text-center">${r.stores}</td>
        <td class="px-3 py-2 text-center">${r.headcount}</td>
        <td class="px-3 py-2 text-center fw-semibold">${r.avgTenureMonths.toFixed(1)} mo</td>
    </tr>`).join('');
}

async function rtLoadManagerTenureTable() {
    const [oc, od, om, soc] = await Promise.all(['oc', 'od', 'om', 'soc'].map(dim =>
        fetch(`/api/retention/average-tenure-by-manager?dimension=${dim}`).then(r => r.ok ? r.json() : [])
    ));
    rtRenderManagerTenureRows('rtOcTableBody', oc);
    rtRenderManagerTenureRows('rtOdTableBody', od);
    rtRenderManagerTenureRows('rtOmTableBody', om);
    rtRenderManagerTenureRows('rtSocMgrTableBody', soc);
    return oc;
}

/* ── Best/Worst 5 by average tenure — store and Operation Consultant. ────── */
function rtRenderTenureRankChart(canvasId, existing, data, color) {
    const ctx = document.getElementById(canvasId);
    if (!ctx) return existing;
    if (existing) existing.destroy();
    return new Chart(ctx.getContext('2d'), {
        type: 'bar',
        data: { labels: data.map(d => d.label), datasets: [{ data: data.map(d => d.value), backgroundColor: color, borderRadius: 4, maxBarThickness: 30 }] },
        options: {
            indexAxis: 'y',
            plugins: {
                legend: { display: false },
                tooltip: { callbacks: { label: c => ` ${c.label}: ${c.formattedValue} months on average` } },
                datalabels: { anchor: 'center', align: 'center', color: '#fff', font: { size: 11, weight: 'bold' }, formatter: v => v > 0 ? v + ' mo' : '' }
            },
            scales: {
                x: { beginAtZero: true, grid: { color: 'oklch(0.92 0.008 255)' }, ticks: { color: 'oklch(0.5 0.03 258)' } },
                y: { grid: { display: false }, ticks: { color: 'oklch(0.5 0.03 258)' } }
            }
        }
    });
}

let rtStoreTenureTopInstance, rtStoreTenureBottomInstance;
async function rtLoadStoreTenureCharts() {
    const r = await fetch(`/api/retention/average-tenure-by-store?${rtFilterQuery().toString()}`);
    const data = r.ok ? await r.json() : [];
    rtStoreTenureTopInstance = rtRenderTenureRankChart('rtStoreTenureTopChart', rtStoreTenureTopInstance, data.slice(0, 5), ChartColors.SEMANTIC.GOOD);
    rtStoreTenureBottomInstance = rtRenderTenureRankChart('rtStoreTenureBottomChart', rtStoreTenureBottomInstance, data.slice(-5).reverse(), ChartColors.SEMANTIC.BAD);
}

let rtOcTenureTopInstance, rtOcTenureBottomInstance;
async function rtLoadOcTenureCharts(ocRows) {
    const data = (ocRows || []).map(r => ({ label: r.name, value: r.avgTenureMonths }));
    rtOcTenureTopInstance = rtRenderTenureRankChart('rtOcTenureTopChart', rtOcTenureTopInstance, data.slice(0, 5), ChartColors.SEMANTIC.GOOD);
    rtOcTenureBottomInstance = rtRenderTenureRankChart('rtOcTenureBottomChart', rtOcTenureBottomInstance, data.slice(-5).reverse(), ChartColors.SEMANTIC.BAD);
}

/* ── How soon people leave, and how much hiring is happening — both always
   full history, so they read clearly even for a young company. ─────────── */
let rtFirstResignInstance;
async function rtLoadFirstResignationChart() {
    const r = await fetch(`/api/retention/time-to-first-resignation?${rtFilterQuery().toString()}`);
    const data = r.ok ? await r.json() : [];
    const total = data.reduce((s, d) => s + d.value, 0);
    const ctx = document.getElementById('rtFirstResignChart');
    if (!ctx) return;
    if (rtFirstResignInstance) rtFirstResignInstance.destroy();
    rtFirstResignInstance = new Chart(ctx.getContext('2d'), {
        type: 'bar',
        data: { labels: data.map(d => d.label), datasets: [{ data: data.map(d => d.value), backgroundColor: ChartColors.colorsByLabel(data.map(d => d.label), null), borderRadius: 6, maxBarThickness: 46 }] },
        options: {
            plugins: {
                legend: { display: false },
                tooltip: { callbacks: { label: c => ` ${c.formattedValue} resignation(s) (${total > 0 ? (c.parsed.y / total * 100).toFixed(0) : 0}% of all resignations)` } },
                datalabels: { anchor: 'center', align: 'center', color: '#fff', font: { size: 11, weight: 'bold' }, formatter: v => v > 0 ? v : '' }
            },
            scales: {
                x: { grid: { display: false }, ticks: { color: 'oklch(0.5 0.03 258)' } },
                y: { beginAtZero: true, ticks: { color: 'oklch(0.5 0.03 258)', precision: 0 }, grid: { color: 'oklch(0.92 0.008 255)' } }
            }
        }
    });
}

let rtHiringVolumeInstance;
async function rtLoadHiringVolumeChart() {
    const q = rtFilterQuery();
    if (rtYear) q.set('sinceYear', rtYear);
    const r = await fetch(`/api/retention/monthly-hiring-volume?${q.toString()}`);
    const data = r.ok ? await r.json() : [];
    const ctx = document.getElementById('rtHiringVolumeChart');
    if (!ctx) return;
    if (rtHiringVolumeInstance) rtHiringVolumeInstance.destroy();
    rtHiringVolumeInstance = new Chart(ctx.getContext('2d'), {
        type: 'line',
        data: { labels: data.map(d => d.label), datasets: [{ data: data.map(d => d.value), borderColor: 'oklch(0.5 0.18 25)', backgroundColor: 'oklch(0.5 0.18 25 / .12)', tension: 0.35, fill: true, pointRadius: 3, pointHoverRadius: 6 }] },
        options: {
            interaction: { mode: 'nearest', intersect: true },
            plugins: {
                legend: { display: false },
                tooltip: { callbacks: { label: c => ` ${c.formattedValue} new hire(s)` } },
                datalabels: { display: false }
            },
            scales: {
                x: { grid: { display: false }, ticks: { color: 'oklch(0.5 0.03 258)' } },
                y: { beginAtZero: true, ticks: { color: 'oklch(0.5 0.03 258)', precision: 0 }, grid: { color: 'oklch(0.92 0.008 255)' } }
            }
        }
    });
}

function rtRenderInsights(items) {
    const el = document.getElementById('rtInsights');
    if (!items || !items.length) { el.innerHTML = '<div class="text-muted" style="font-size:13px">' + rtL.noInsights + '</div>'; return; }
    const colorMap = {
        danger:    { bg: 'oklch(0.6 0.22 22 / .08)', border: ChartColors.SEMANTIC.BAD, text: ChartColors.SEMANTIC.BAD },
        success:   { bg: 'oklch(0.62 0.15 155 / .1)', border: ChartColors.SEMANTIC.GOOD, text: ChartColors.SEMANTIC.GOOD },
        warning:   { bg: 'oklch(0.75 0.16 75 / .12)', border: ChartColors.SEMANTIC.WARNING, text: 'oklch(0.6 0.14 75)' },
        secondary: { bg: 'var(--bg-2)', border: 'var(--text-3)', text: 'var(--text-2)' }
    };
    el.innerHTML = '<div class="row g-3">' + items.map(item => {
        const c = colorMap[item.color] || colorMap.secondary;
        return `<div class="col-md-6">
            <div style="background:${c.bg};border-left:3px solid ${c.border};border-radius:8px;padding:12px 14px;height:100%">
                <div class="d-flex align-items-start gap-2">
                    <i class="bi ${item.icon} mt-1 flex-shrink-0" style="color:${c.text};font-size:15px"></i>
                    <div>
                        <div style="font-size:12.5px;font-weight:700;color:${c.text};margin-bottom:2px">${sapEscape(item.title)}</div>
                        <div style="font-size:12px;color:var(--text-2);line-height:1.5">${sapEscape(item.description)}</div>
                    </div>
                </div>
            </div>
        </div>`;
    }).join('') + '</div>';
}

async function rtLoadInsights() {
    rtRenderInsights(await rtFetch('insights'));
}

async function rtLoadAll() {
    if (!rtYear || !rtMonths.length) return;
    const [ocRows] = await Promise.all([
        rtLoadManagerTenureTable(),
        rtLoadMilestones(), rtLoadSurvivalCurve(), rtLoadLeaderboard(), rtLoadTenureByStore(), rtLoadInsights(),
        rtLoadJobTitleChart(), rtLoadGenderChart(), rtLoadStoreTenureCharts(),
        rtLoadFirstResignationChart(), rtLoadHiringVolumeChart(),
    ]);
    await rtLoadOcTenureCharts(ocRows);
}

(async function init() {
    const hasData = await rtLoadFilters();
    if (hasData) await rtLoadAll();
})();
</script>
